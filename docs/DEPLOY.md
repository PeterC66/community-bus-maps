# Deploying and running the portal (P7)

<!-- docstamp v1.123 | 2026-09-26 | sha=88b57a87 -->
**v1.123** · updated 26 September 2026

Small service, deliberately: **one Node process, one SQLite file, one data volume.** No database server, no queue, no build step. Scale by giving the VM more disk, not by adding components — the plan says single-VM until something actually binds.

Everything below assumes the decisions already locked in the planning docs: build locally, choose the host at launch; the render pipeline must stay byte-identical.

---

## 1. What has to persist

| Path (under `DATA_DIR`) | What it is | Backed up? |
|---|---|---|
| `portal.sqlite` (+ `-wal`, `-shm`) | customers, users, maps, versions, publish requests, proposed updates, audit | **yes** |
| `maps/<id>/data/` | the map's payload + generators + expert `diagram-layout.json` | **yes** |
| `maps/<id>/overrides.json` | the customer's canonical safe-subset edits | **yes** |
| `maps/<id>/renders/v*/` | every rendered version, **including the bytes an approver reviewed** | **yes** |
| `maps/<id>/proposed/<pid>/` | a staged monthly refresh | no — re-stageable centrally |
| `maps/<id>/archive/` | data replaced by an accepted refresh | no — superseded |

Nothing in this list is in git, and nothing in git is needed at runtime beyond the application itself.

## 2. Environment

Copy `.env.example`. The ones that matter in production:

| Variable | Why |
|---|---|
| `DATA_DIR` | the volume above. **Set it explicitly** — the default is `./data` inside the app dir |
| `HOST` / `PORT` | bind address; behind a proxy, `127.0.0.1:5180` |
| `PUBLIC_BASE_URL` | the public origin, used by `robots.txt` + `sitemap.xml` (P6) |
| `EMAIL_PROVIDER` / `EMAIL_FROM` / `RESEND_API_KEY` | magic-link delivery via Resend (`src/email/resend.js`, the only provider wired up). **Unset `EMAIL_PROVIDER` means sign-in links are printed to the log** — fine in dev, not in production. Needs SPF/DKIM at 20i for `EMAIL_FROM`'s domain. |
| `METRICS_TOKEN` | set to expose `/metrics`; unset and the endpoint 404s |
| `STATUS_TOKEN` | set to accept `POST /api/admin/status` (the laptop's `push-status.mjs`); unset and it 404s |
| `OPERATOR_TOKEN` | set to let the laptop's bus-work worklist READ `GET /api/admin/worklist`, `GET /api/maps` and, since 2026-09-05, `GET /api/maps/:id/poi-tiers` (a map's landmark answer, buses-data OA-233) without a person's session (OA-203); unset and no operator is admitted. Bearer header only, GET only, those three routes only — it replaces a live `cbm_session` cookie kept in a file, which could do everything an admin can bar the four step-up routes |
| `PILOT_MODE` | **defaults ON.** Banner on every page and a band on every rendered sheet. Set to `0` to switch all of it off — see [`PILOT.md`](PILOT.md). Does **not** control indexing; that is `ALLOW_INDEXING` below |
| `ALLOW_INDEXING` | **defaults OFF.** While off, `robots.txt` serves `Disallow: /` and the site cannot be found in a search engine. Set to `1` to become discoverable. Independent of `PILOT_MODE` since 2026-08-21, so the site can be indexed while still honestly labelled a pilot — see `src/config.js` §INDEXING |
| `TUBE_DIAGRAM` | **defaults OFF, and stays off on the live host.** The tube-map diagram output was parked on 2026-09-10 (buses-data OA-297): off, it is not rendered, not listed, not offered in the editor and not served by the public file routes. Set to `1` on a dev copy to test the pin editor and the request-only lock; the return path is buses-data OA-298 |
| `ALLOW_SELF_APPROVAL` | **must be `1` on this host today, or nothing can be published.** Since 2026-08-20 an approver who submitted a version is refused when they approve it (`technical-audit_2026-08-19` S6). With one operator that means every publication, so the override is set — and each publication made under it is stamped `selfApproved: true` in the evidence and the audit row. Unset it the day a second person holds `approver`. |
| `ADMIN_EMAIL` | the address `npm run smoke:signin` sends its one real magic link to after a deploy. Must be a **registered, active** user, or no send is attempted and the check cannot pass. |
| `BACKUP_RECIPIENT` | an **age public key** (`age1…`). Set it and `scripts/backup.mjs` writes the database copy encrypted, as `portal.sqlite.age`; leave it unset and the backup still runs but writes the database in the clear and says so on every run (`technical-audit_2026-08-25` N3). The matching **private key never goes on this host** — it lives in the operator's password manager, which is the whole point of an asymmetric key here. §5 below has the restore command. **Putting it in `.env` is not enough**: the `backup` service has no `env_file`, so `compose.yaml` must name it too, and it does — setting one without the other leaves the key looking correct in `.env` while every backup goes on writing plaintext. Prove it reached the container with `docker compose run --rm backup printenv BACKUP_RECIPIENT`, not by reading `.env`. |

**Setting one of these in `.env` is only half of it, and the other half is silent.** The `portal` service in [`compose.yaml`](../compose.yaml) has no `env_file`: Compose reads `.env` only to substitute `${VARS}` into that file, so **a variable that is not also named under `environment:` there never reaches the container**, however correctly it is spelled. The app then takes its unset branch while the value sits in `.env` looking right, and nothing says the two disagree. It has caught four variables so far — `ALLOW_SELF_APPROVAL` and `ALLOW_INDEXING` (2026-08-21), `BACKUP_RECIPIENT` (2026-08-25) and `OPERATOR_TOKEN` (2026-08-31), the last of them by somebody who had read the warning in `compose.yaml` an hour earlier. That is the point: the warning was in the file nobody opens to add a variable. `npm run check:compose-env` now refuses the omission, names the variable and the file that reads it, and runs inside `npm test`. Prove it can still fail by deleting one variable's line from `compose.yaml` and running it — that is how it was falsified against the real `OPERATOR_TOKEN` fault on the day it was written.

### Rotating a token

`METRICS_TOKEN`, `STATUS_TOKEN` and `OPERATOR_TOKEN` are values this operation generates for itself, so rotating one is a scripted job rather than a recipe to follow by hand. Run it from the **repository root** (the folder holding `package.json`); the only thing to substitute is the variable name, and it must be one of those three:

```bash
npm run rotate:secret -- METRICS_TOKEN
```

Add `--dry-run` to see the preflight and change nothing. `RESEND_API_KEY` is deliberately **not** rotatable this way — it is issued by Resend, and 48 random hex characters would authenticate to nothing while presenting as sign-in emails that quietly stop arriving.

**The new value is generated on the host and never comes back.** `openssl rand -hex 24` runs inside the remote shell and the result is written by a `printf` builtin, so it is never a process argument visible in `ps`, never echoed, and never enters shell history at either end — the script installs a value it does not itself see. Read it once from the host's `.env` if something else needs a copy.

**It proves the rotation rather than reporting it**, in the three ways this system has had to learn. The file and the running process are compared by `sha256` fingerprint and must agree with each other and differ from the old one — a config read cannot tell you a container was recreated, and `docker compose up -d` is used rather than `restart`, which does not re-evaluate the `${VAR}` substitution. Then the **old** value is exercised against the live service and its refusal asserted: for `METRICS_TOKEN` that is `/health?deep=1` losing `gitSha`, for `OPERATOR_TOKEN` a 401 on `/api/admin/worklist`. `STATUS_TOKEN` has no safe read-only route — the only thing it opens is a POST that writes the status snapshot — so its revocation is reported **UNPROVED** rather than inferred from a config read.

**It keeps no `.env` backup, on purpose.** The obvious `cp .env .env.bak-$(date)` leaves a cleartext duplicate of every *other* live secret sitting beside the original for ever; two such files were found on this host on 2026-09-01, one of them five weeks old and holding a live `RESEND_API_KEY`. The only thing such a backup preserves is a value being retired deliberately, so the old fingerprint is printed instead. Because that fingerprint is the only record, it is taken the way Compose reads the key — the last line for a repeated key, one pair of quotes stripped — through the same `scripts/lib/host-env.mjs` reader the deploy uses. Until 2026-09-24 it took the first line and kept the quotes, so on a duplicated or hand-quoted key it described a value that was never live (buses-data OA-425).

**Rotating `STATUS_TOKEN` or `OPERATOR_TOKEN` breaks a laptop-side tool until its copy is updated** — `push-status.mjs` and `worklist.mjs` in the `bus-work` skill. The script says so in its preflight, before it changes anything, rather than at the end. `npm run test:rotate-secret` asserts every refusal above and breaks the `compose.yaml` preflight on purpose to show it can go red. It also runs the host script's read of the old value under a real `sh` against fixture `.env` files — plain, repeated, quoted, Windows line endings — and requires the fingerprint to match the value Compose would load, with the old first-line read put back on purpose to show that arm can go red too.

## 3. Run it

```bash
export GIT_SHA=$(git rev-parse --short HEAD) BUILT_AT=$(date -u +%FT%TZ)
docker compose up -d --build
```

The two exports stamp the build into `/health`, the site footer and every render's `meta.json` (the go-live record, now history at [`_archive/GO-LIVE.md`](_archive/GO-LIVE.md), §5) — harmless to skip, but then those fields all read `unknown`/blank.

Or without containers (systemd on a plain VM):

```ini
# /etc/systemd/system/cbm-portal.service
[Unit]
Description=BusMaps.uk portal
After=network.target

[Service]
Type=simple
User=cbm
WorkingDirectory=/opt/community-bus-maps
Environment=NODE_ENV=production
Environment=DATA_DIR=/var/lib/cbm
Environment=HOST=127.0.0.1
Environment=PORT=5180
ExecStart=/usr/bin/node src/server.js
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

Put nginx/Caddy in front for TLS and the public hostname. Two proxy details the app relies on: forward `X-Forwarded-Proto` (the session cookie is marked `Secure` when the request is HTTPS) and don't strip `/api/` or `/m/` paths.

### 3a. Security headers live in the Caddyfile, and they are deployed separately

The `Caddyfile` in this repo carries the site's security headers - HSTS, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy` and a strict Content-Security-Policy (added 2026-08-19; until then every response carried none of them, `technical-audit_2026-08-19` S1). **This file is not deployed by `deliver-map.mjs`, `deploy.mjs` or the container build.** Caddy runs on the host, outside Docker, so shipping a new app image does nothing to it. If you change the `Caddyfile` you must copy and reload it yourself, or the change simply never happens.

**The whole thing is one command.** From `C:\Claude\community-bus-maps` (the repo root):

```bash
npm run deploy:caddy
```

That copies the `Caddyfile` up, installs it, runs `caddy validate`, reloads Caddy only if validation passed, and then reads the live headers back to prove the reload actually took effect. It gets the host and key from `.env`, so there is nothing to look up. To check the live headers without changing anything:

```bash
npm run deploy:caddy -- --check
```

**Why the verify step is not optional:** a reload that silently kept the old config looks exactly like a successful one. On 2026-08-20 the app was deployed and the `Caddyfile` was not, and the site ran for a while with the headers merged, the deploy reported done, and every response still carrying none of them. Only reading the headers back tells the two apart.

The manual sequence below is what that command does, kept for when something goes wrong in the middle of it.

**Getting onto the VPS from the laptop.** From `C:\Claude\community-bus-maps` (the repo root):

```bash
npm run ssh
```

That reads `DEPLOY_HOST`, `DEPLOY_SSH_KEY` and `DEPLOY_APP_DIR` out of `.env` and drops you into a shell already in the app directory, so there is no hostname or key path to remember. To run a single command without opening a shell, quote it as one argument after `--`:

```bash
npm run ssh -- "docker compose ps"
```

**Copying the Caddyfile up.** Also from `C:\Claude\community-bus-maps`, and note the trailing `:` on the destination - it means "your home directory on that host". `%DEPLOY_HOST%` is a placeholder for whatever `DEPLOY_HOST` is set to in `.env` (of the form `user@host`); substitute it by hand, since `scp` does not read `.env`:

```bash
scp Caddyfile %DEPLOY_HOST%:
```

**Then, in the shell that `npm run ssh` opened**, install and reload it. `~/Caddyfile` is where the `scp` above just put it; `/etc/caddy/Caddyfile` is fixed and is where Caddy actually reads from:

```bash
sudo cp ~/Caddyfile /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl reload caddy
```

`caddy validate` before `reload` on purpose: `reload` on a malformed file leaves the previous config running on some versions and fails the site on others, and finding out which by experiment is not a thing to do to a live site.

Then check from the **laptop** that the headers actually arrive, because a reload that silently kept the old config looks exactly like success:

```bash
curl -sI https://busmaps.uk/ | grep -iE "content-security-policy|strict-transport|x-content-type|referrer-policy|permissions-policy"
```

The CSP is strict (`script-src 'self'`, no `'unsafe-inline'`) and the site is built to stay inside it: there are **no inline `<script>` blocks anywhere in `public/`**, and three that used to exist were moved out to `/js/contact-kind.js`, `/js/app-dashboard.js` and `/js/app-login.js` on 2026-08-19 to keep it that way. If you add an inline script, the browser will silently refuse to run it and the page will half-work. Add a file under `public/js/` and a `<script src=... defer>` instead. After any change to a page's scripts, open `/`, `/maps`, `/m/<slug>` (any published map - `<slug>` comes from `/api/public/maps`), `/app/login.html` and a signed-in `/app`, and confirm the browser console shows no `Refused to ...` lines.

There is a way to test the whole header block without touching the live site or installing Caddy locally: run the portal (`npm start`, from the repo root) and put a proxy in front of it that reads the header values straight out of the `Caddyfile`. That is how this block was checked before it was first deployed - including confirming the CSP *blocks* an injected inline script, rather than only confirming the pages still load.

### 3b. The deploy-history entry is written BEFORE the deploy, and merged with it

**Who runs this.** Since 2026-09-17 (buses-data OA-394, R1 of the process review) a Claude session or the scheduled loop may merge a green pull request and run the deploy itself, from any folder on the laptop, with no placeholders:

```bash
npm --prefix "C:/Claude/community-bus-maps" run deploy
```

It does so only when CI is green on `main` for the commit being deployed, only after the history entry below has been merged, and only if it then reads the result back: `X-App-Version` from `https://busmaps.uk/` and `/health?deep=1` (§4). If the read-back fails, the fix is a **revert by a second pull request**, merged and deployed the same way — never a hand edit on the host. **Every raw `ssh` or `scp` to the VPS stays Peter's**, because Claude Code's auto-mode classifier refuses them to sessions: the token-holding `/health` read-back in §4, the `scp` of the §5 drills, and any `npm run ssh -- …` such as R1's `delete-map`. `npm run deploy` wraps its own `ssh` calls and is the one route a session uses, and since 2026-09-26 it also runs the §4a tracking itself, so a re-vendor no longer owes Peter a hand step. **A deploy that is pending is a chore, not a fault**: the bus-work worklist carries it as a row (`deploy_pending.mjs` in the `bus-work` skill), and the board never goes red for it.

Adopted 7 September 2026 (buses-data OA-266, retired the same day). **Write the entry, merge it, then deploy the merge.** The old order — deploy, then record it — is what put `main` permanently one commit ahead of the live site, because a bookkeeping commit lands *after* the thing it describes and nothing deploys it. That is not a nuisance: `status.js`'s deployment check compares the live commit against `main`, so **the act of documenting a deploy was what made the next drift red**. On 2026-09-06 that clock ran out at 20:14 and produced seven consecutive CI failures across two repositories, and `buses-data` is private, so each inherited red was a billed run whose verdict was about paperwork rather than about the site.

**The sha an entry names is the change, and the sha that gets deployed is the merge carrying the entry.** Those are two different commits and always will be; say which is which in the entry rather than leaving a reader to work it out. Where the change is small enough to share a pull request with its own history entry, put both in one PR and the two collapse into one.

**The entry is a `CHANGELOG.d/` fragment in the change's own pull request, and the deploy refuses a commit that has none** (buses-data OA-377, adopted 2026-09-26). This ordering fixes where an entry sits against ITS OWN change and used to say nothing about every other commit that merged between the writing and the deploy: twice in September 2026 those went live described by nothing (#295–#297, then #301–#303, the owner picker among them), and both times it was the next entry's author who noticed. So `npm run deploy` now fetches and runs `scripts/check-deploy-history.mjs` as its step 0, before the backup, and stops if any first-parent commit on `origin/main` after the check's baseline neither adds a fragment, nor edits this file, nor is named by `#NNN` or short sha in one of them. When it stops, it names each commit; the remedy is a fragment naming them, in its own pull request, merged and then deployed. To ask the same question without deploying, from the repository root (`C:\Claude\community-bus-maps`), with no placeholders:

```bash
npm run check:deploy-history
```

It exits 0 when everything is described, 1 when something is not, and 2 when git cannot answer. It reads git and the documents only, never the clock or the live site, so it can say nothing about what the host is ACTUALLY running; that is still `X-App-Version`, below. The *Deploy history* line in §9 has not been written to since 2026-09-25, because the fragments now carry every entry; it stays as the record up to then, and the check still reads it.

**What an entry written in advance cannot hold is the post-deploy evidence** — `gitSha`, `builtAt`, the `check:live-routes` count, the §3a read-back. That is a real cost of this ordering and it is accepted deliberately, but **three of those four readings are reproducible from the running site at any time and the fourth is not**. The sha, the `check:live-routes` count and the §3a read-back need no credential — §4's `X-App-Version` header carries the sha to any caller, which is how the `gitSha` line is reproduced on a machine holding no token. **`builtAt` is the one reading that must be captured at deploy time**: it is served only in the `opsAuthorised()` branch of `/health`, so a caller without `METRICS_TOKEN` cannot ask for it later, and this laptop deliberately holds none. Against that, the drift red this ordering replaces was silent for twelve hours and then woke somebody at 20:14. So write what is known in advance — what is shipping, why, and what §3a check will be the one that can see it — and add a **second** entry only when the deploy fails or the read-back surprises you. A failure is loud and immediate; that is the ordering answering the obvious objection that a pre-written record claims a deploy that has not happened yet.

## 4. Smoke test after every deploy

```bash
curl -fsS localhost:5180/health?deep=1 | head -40
```

`?deep=1` is the **readiness** probe, not a ping: it queries the database, writes and deletes a probe file in `DATA_DIR`, checks the vendored engine files (including the P7 expert styles), rasterises a tiny image through sharp, and — since 2026-08-20 — checks that email is **configured** (`technical-audit_2026-08-19` O4: sign-in is the only door into this system, and a deployment that cannot email is one nobody can sign in to). It returns **503** if any of those fail, so it is what a load balancer and the container `HEALTHCHECK` should use.

**What it tells whom (S4, done 2026-08-20).** An anonymous caller now gets four fields — `status`, `service`, `version`, `time` — and nothing else. It used to hand anybody the git SHA, the build time, the exact sharp and libvips versions, the object-store path and eleven business counts, which is a CVE-targeting aid plus a public read-out of how small the operation is. Send an `Authorization: Bearer` header carrying `METRICS_TOKEN` (or use an admin session) to get the whole thing back, including `checks{}`.

**The `?token=` query form was removed on 2026-08-25** (technical-audit_2026-08-25 N7). The Caddyfile enables an access log and Caddy records the full request URI, so every use of the query form wrote a live credential in clear into `/var/log/caddy/busmaps.access.log` — a file in no backup and, at that point, under no retention rule anybody had chosen. The server no longer accepts it. **Two things about that file changed on 2026-09-06** (buses-data OA-086 phase 1): it now states its own rolling — 10 MiB, 10 files, 30 days — where before it silently inherited Caddy's 90-day default, and the visitor's address is masked in **both** the fields Caddy writes it to, `remote_ip` and `client_ip`. `/legal.html` promises the public both of those, so neither moves without that page moving with it, and `scripts/test-caddyfile.mjs` holds this file to the retention and to the same prefix `src/public/logRedaction.js` masks the app's own log with. Run this from **any folder on any machine**; `$METRICS_TOKEN` is read from your own shell environment and there is nothing else to substitute:

```bash
curl -fsS -H "Authorization: Bearer $METRICS_TOKEN" "https://busmaps.uk/health?deep=1"
```

**That command needs a `METRICS_TOKEN` in YOUR shell, and the laptop deliberately has none** — the local copy was retired on 2026-09-01 after the value leaked twice, so on this machine the line above returns the four-field anonymous answer and no `gitSha`. The token lives on the host, which is where to ask from. This is one self-contained command, run from any folder on the laptop; every path in it is real and nothing is a placeholder, and the value is read inside the remote shell so it never becomes an `ssh` argument visible in `ps` or a line in either machine's history:

```bash
ssh -i "C:/Users/Peter/.ssh/busmaps_vps" ubuntu@51.38.80.87 'cd /opt/community-bus-maps && MT=$(sed -n "s/^METRICS_TOKEN=//p" .env) && curl -fsS -H "Authorization: Bearer $MT" "localhost:5180/health?deep=1"'
```

**`sed`, not `.` — do not source that file.** `.env` is Compose's key/value format and is not shell: `EMAIL_FROM=Peter Cooper - BusMaps.uk <info@busmaps.uk>` is a valid line in it and a syntax error to a shell, so sourcing dies on line 3 and never reaches `METRICS_TOKEN` on line 11. That is not hypothetical — it is what `npm run deploy` did until 2026-09-19, silently, while reporting that the host had no token at all.

**The verdict stayed public on purpose, so nothing had to be reconfigured.** `?deep=1` still runs for an anonymous caller and still returns 503 when a dependency is down; only the per-check detail is gated. That is what the warning in the previous version of this section was about — gating the verdict would have turned every Uptime Robot poll into a 401 and paged about a fault that did not exist. Do not undo that without changing the monitor and the Dockerfile `HEALTHCHECK` in the same commit.

This URL is monitored from outside: an **Uptime Robot** check polls `https://busmaps.uk/health?deep=1` every five minutes and alerts by email (set up 2026-08-20, `technical-audit_2026-08-19` O2 — the alert address is recorded in `community-bus-maps-ops`, not here). It is not a free ping, so tightening the interval multiplies the rasterisations this box does.

**Readiness is not proof that anyone can sign in.** It checks that email is configured, not that the provider will accept a message today. `npm run deploy` therefore ends by running:

```bash
npm run smoke:signin
```

from the repo root on the laptop (`C:\Claude\community-bus-maps`). It POSTs one real sign-in request for `ADMIN_EMAIL` to the running service and then reads the **server log** for `magic link emailed` — the HTTP response is deliberately identical whether or not the address is registered, so it cannot be the evidence. It exits non-zero on a 503, on a provider that threw, on a link that went to the console instead of an inbox, and on no matching log line at all. `npm run deploy -- --skip-signin` skips it, and then nothing has proved a real sign-in email can be sent.

**And ask whether every route is still there, because nothing else can.** `/health?deep=1` asks whether the process came up, the byte gates ask whether the drawn output still matches, and `npm test` asks the laptop's build — none of them can see a route that stopped being registered, and a route that stops being registered does not throw. It 404s, quietly, to whoever asks for it next. Run this from the repository root (`C:\Claude\community-bus-maps`); it defaults to the live site and takes no placeholders:

```bash
npm run check:live-routes
```

It asks the RUNNING site, anonymously, about every route in `scripts/route-table.json` — the snapshot recorded from the unsplit server and owned by `test-admin-plugin.mjs`, so the list comes from something other than the app under test. It is safe against production: guarded routes refuse before doing anything, and the six a stranger may legitimately POST to are sent an empty body that each refuses without creating a row. Two different things return 404 and it tells them apart **by the body**, which was measured rather than assumed: `{"message":"Route GET:/x not found"...}` is the router saying the route is GONE, while `{"ok":false,"error":"No published map with that name."}` is the app answering correctly about a slug nobody published. It also catches the opposite fault — a route that is still there and answers **2xx** to a stranger — and it **exits 2 on a route it has no rule for** rather than skipping it, so the check cannot quietly shrink as routes are added. `npm run test:prove-red-live-routes` breaks a route, a guard, a page redirect and a rule in turn against a scratch server on a free port, and requires each to redden the message that names it.

**It was written after doing it by hand twice** — four routes after `94773e3` and twenty-eight after `fd438a6`, both times because OA-231 had moved routes between files and no gate in any of the three repositories could see a lost one.

**And ask the same question of the URLs the site OFFERS a crawler, which is a different population from the route table.** The check above asks whether every route we registered still answers; this one asks whether every URL `sitemap.xml` advertises answers, on this host, saying which URL it is. Run it from the repository root (`C:\Claude\community-bus-maps`); it defaults to the live site and takes no placeholders:

```bash
npm run check:sitemap
```

Add `-- --base http://127.0.0.1:5180` to ask a local instance, and `-- --verbose` to list the sound URLs as well as the faults. It exits 1 on a URL that answers wrongly and **2 on a sitemap it could not fetch, could not parse, or that advertises nothing** — a check with no population must never report clear. **It found a real one on the day it was written** (buses-data OA-284): `/o/busmaps-uk-pilot` answered 200 and carried no `<link rel="canonical">` at all, because the route ended in a bare `sendFile` of a shell whose title and description are the same for every organisation that will ever have a page. 47 of 48 URLs were perfect, which is exactly the shape that survives a spot check. **The duplicate-identity half of the rule is reported here and ASSERTED offline**, by `npm run test:sitemap`, which seeds two organisations — with one organisation there is nothing to be a duplicate of, so a green here would as often mean *there was only one page of this kind* as *they differ*, and the run says so rather than letting a weak reading pass for a strong one. `npm run test:prove-red-sitemap` breaks the page six ways against a copy of the repository, including the arm where the sitemap stops advertising organisations at all.

Then, signed in as an admin, open **`/app/admin` → Ops**: dependency health, per-map disk usage, what a prune could reclaim, and the activity counts. Same numbers as `/metrics` (Prometheus text, gated by `METRICS_TOKEN` or an admin session).

### 4a. A re-vendor is not finished until the LIVE store has been tracked, and the live number is not the laptop's

**A delivered map's data pack carries its own copy of the generators and `generateSvg()` runs THAT copy** (OA-130: track, not freeze). So deploying a re-vendored `engine/` reaches the portal and does not reach the maps already in it. `scripts/track-engine.mjs` is what closes that, it re-renders nothing, and no published byte changes — what changes is the NEXT render of each map.

**Run it where the maps are, and read that number.** The script's store comes from `MAPS_DIR`, which derives from `DATA_DIR` — on the laptop that is the local dev store, which is a DIFFERENT set of maps from the live one. Measured 2026-09-02 after deploying `a65d9a7`: the laptop said *3 already current, 10 BEHIND*; the live store said **10 already current, 36 BEHIND**. A local run is not an answer about busmaps.uk, and a green local run after a re-vendor is the easiest way to believe the hand-off is done when 36 packs are still frozen at import.

**`npm run deploy` does this itself, as its step 5b, on every deploy (since 2026-09-26, buses-data OA-473).** After the switch and the readiness check it runs `track-engine.mjs --apply` inside the live container and then the report form, and its verdict is the exit code of that second run — a read of the store after the write, not the absence of an error from the write. On a deploy that moved no engine file it copies nothing and prints `0 BEHIND`. If anything is left behind, or any row is a `?`, the deploy still runs the sign-in test and then exits non-zero with the site live, so nobody reads it as done. Until then this was two raw `ssh` commands that only Peter could run, owed after every re-vendor and surfaced by nothing on the board; five re-vendors (#373, #381, #384, #386, #389, 25–26 Sep 2026) were waiting on it at once. `scripts/lib/track-live.mjs` holds the step and `npm run test:deploy-track-engine` runs the real `track-engine.mjs` against scratch stores to prove it, including the arm where the write succeeds and the read-back says behind.

The two commands stay here for Peter, for reading the live store without deploying — report first, then apply, then re-read. From any folder on the laptop; the host path is real and there are no placeholders:

```bash
ssh -i C:/Users/Peter/.ssh/busmaps_vps ubuntu@51.38.80.87 "cd /opt/community-bus-maps && docker compose exec -T portal node scripts/track-engine.mjs"
```

```bash
ssh -i C:/Users/Peter/.ssh/busmaps_vps ubuntu@51.38.80.87 "cd /opt/community-bus-maps && docker compose exec -T portal node scripts/track-engine.mjs --apply"
```

The reporting form exits non-zero while any pack is behind, so it works as a check as well as a report. The state to finish on is **`0 BEHIND, 0 skipped`** — a `skipped` pack is one whose `engine-source.json` does not say which external generator it was built from, and it is skipped rather than guessed because overwriting a map with the wrong generator is a silent corruption found at the next render. **Read the marks, not only the counts (2026-09-02):** a `·` skip is that benign not-recorded case, but a `?` skip is a pack naming a generator this engine does not vendor, or a declaration that will not parse — neither is fixed by `--apply`, and since 2026-09-02 either makes the run exit non-zero. Until then both were only `skipped`, and the exit code read `BEHIND` alone: a pack declaring a dropped generator printed the words "which is not vendored" and passed.

**And `npm run check:vendored --skills` answers about the DISK, not about the commit.** It compares `engine/` with the skill sources in a working tree, so in this account's shared checkout another session's uncommitted edits read as `DRIFTED` on files your deploy never touched. That happened on 2026-09-02: four files reported drifted and all four matched the skills tree at `HEAD` byte-for-byte. Before believing a drift row, compare against `git show HEAD:<path>` in the skills repo.

## 5. Backups

```bash
npm run backup -- --out /backups --keep-days 14 --keep 7   # or: docker compose run --rm backup
```

**The database copy is encrypted** when `BACKUP_RECIPIENT` is set (an age public key — see §2). The file is then `portal.sqlite.age` and every restore below starts with one extra command, run **on the laptop, from wherever the backup folder is**, using the private key from your password manager:

```bash
age -d -i backup-age-key.txt -o portal.sqlite portal.sqlite.age
```

`backup-age-key.txt` is the identity file holding that private key; `portal.sqlite.age` is the encrypted copy inside the snapshot folder you are restoring from. `manifest.json` in that folder records which public key it was encrypted to, under `dbEncryptedTo`, so a snapshot found in two years says which key opens it. Only the database is encrypted — `maps/` is published material and restores unchanged (`technical-audit_2026-08-25` N3).

The database is copied with SQLite's `VACUUM INTO`, which writes a **consistent** copy of a committed state while the server is running — copying `portal.sqlite` with `cp` under WAL can capture a torn file plus a stale `-wal`, so don't. Each run writes `<out>/<timestamp>/` with `portal.sqlite`, `maps/<id>/{data,renders,overrides.json}` and a `manifest.json`, then prunes on the rule in [`src/ops/backup-retention.js`](../src/ops/backup-retention.js): anything older than `--keep-days` goes, snapshots older than `--keep-recent-hours` are thinned to the newest of each UTC day, and the newest `--keep` are held whatever their age. **`--keep` is a floor, not the window.** Until 2026-08-28 it was the whole rule and meant 14 *folders*: `npm run deploy` takes a backup before every release, so a dozen manual runs in one working day cut the retention window from a fortnight to 25 hours, and it did exactly that on 2026-08-27. Run it from cron/a timer daily, and keep at least one copy **off the box**.

### Drill the DECRYPTION first, and do it without touching the live site

**An untried decryption is not a backup.** The restore below was performed for real on 2026-08-09 — *before* encryption existed. `BACKUP_RECIPIENT` arrived on 2026-08-25, since when every snapshot's database is `portal.sqlite.age` and every restore starts with an `age -d` that had never been run on a real snapshot (codebase review 2026-09-01, portal-ops B1). That failure mode is the worst available: it is discovered on the day the database is gone, and the answer about the key arrives months too late to do anything about it.

`scripts/restore-drill.mjs` answers the only question the drill exists for — *does this snapshot become a working database?* — in a temporary directory. It decrypts, runs `PRAGMA integrity_check` and `foreign_key_check`, then **opens the result with `src/db/index.js`**, which is what runs the migrations and is the step that turns *the file decrypted* into *this release can serve it*; then it counts the rows that would tell you a restore had silently produced an empty database, and checks `maps/` against the manifest. It never stops a container, never writes to `DATA_DIR`, and never touches the live volume — that half stays a human operation, below, because a script that can replace the live database is a script that can replace it by accident.

**Do not copy an angle-bracket placeholder into a shell.** PowerShell treats `<` as a reserved operator and refuses the line with *The '<' operator is reserved for future use* — which is what happened the first time this section was written, on 2026-09-03. Every command below carries **real paths**, and the two you have to change are named after it.

**Step 1 — copy a snapshot down from the host.** It is read, never modified. `maps/` is most of a snapshot's half-gigabyte and is not encrypted anyway, so for a decryption drill copy just the manifest and the database. Run from any folder; the only thing to change is the timestamp folder name, which you can list with `ssh -i C:/Users/Peter/.ssh/busmaps_vps ubuntu@51.38.80.87 "ls /opt/community-bus-maps/backups"`:

```bash
scp -i C:/Users/Peter/.ssh/busmaps_vps ubuntu@51.38.80.87:/opt/community-bus-maps/backups/2026-09-02T22-29-58/manifest.json ubuntu@51.38.80.87:/opt/community-bus-maps/backups/2026-09-02T22-29-58/portal.sqlite.age C:/Claude/community-bus-maps/backups/live-2026-09-02T22-29-58/
```

**Step 2 — put the private key in a file.** From the password manager, into a plain text file. `age` reads the file itself, so the key never passes through Node, is never printed and is never logged. Put it somewhere outside the repository — `C:/Users/Peter/age-backup-key.txt` below — and delete it when you are done.

**Step 3 — run the drill**, from the repository root (`C:\Claude\community-bus-maps`):

```bash
node scripts/restore-drill.mjs --snapshot C:/Claude/community-bus-maps/backups/live-2026-09-02T22-29-58 --identity C:/Users/Peter/age-backup-key.txt --db-only
```

**The two things to change** are the snapshot folder (whatever you called it in step 1) and the identity file (wherever you put the key in step 2). Nothing else in that line is a placeholder.

`--db-only` says you copied the database and not `maps/`; the run then SKIPS the maps check out loud and names how many maps went unverified, rather than passing quietly. Drop it if you copied the whole snapshot. Exit 0 the snapshot restores, 1 it does not, 2 the arguments are wrong. Add `--keep` to leave the decrypted database for inspection — it is a full copy of live data, so delete it afterwards.

**Falsified before it was trusted, 2026-09-03**: run against a snapshot encrypted to a throwaway key it reports every check green through the migrations and the row counts; run against the *same* snapshot with a different identity it fails on `age -d`, names the recipient the manifest records, and exits 1.

**And then run for real, the same day.** The live `2026-09-02T22-29-58` snapshot, with the key from the password manager, passed every check. That is the first time an encrypted snapshot has been opened since `BACKUP_RECIPIENT` was added on 2026-08-25 — for nine days the backups were, strictly, untested. **Re-drill whenever `BACKUP_RECIPIENT` changes**, which is the only event that can invalidate this result, and add a row to the table below; a rotated key with no drill behind it is the same unknown all over again. Delete the identity file afterwards.

| Drilled | What | Result |
|---|---|---|
| 2026-08-09 | Full restore into the live volume, unencrypted database | Passed; corrected this section's volume assumption |
| 2026-09-03 | `restore-drill.mjs` end-to-end on an encrypted snapshot, throwaway key | Passed, and seen to fail on the wrong key |
| 2026-09-03 | `restore-drill.mjs` on the **live** `2026-09-02T22-29-58` snapshot with the real `BACKUP_RECIPIENT` key, `--db-only` | **Passed, every check green.** The first time an encrypted snapshot had been opened since `BACKUP_RECIPIENT` was added on 2026-08-25 |

### Restore drill (do it once, before you need it)

If running under `compose.yaml` as written (a named volume, not a bind mount at `/var/lib/cbm` —
that path was this section's original assumption; performed for real 2026-08-09 and corrected to
match what's actually deployed):

```bash
docker compose stop portal

# Snapshot the live volume for diagnosis before touching it, then restore from a backup.
# (DATA_DIR is /data inside the container; the volume name is <project>_portal-data.)
docker run --rm -v community-bus-maps_portal-data:/data \
  -v /opt/community-bus-maps/backups:/backups:ro alpine sh -c '
    mkdir -p /tmp/broken && cp -a /data/portal.sqlite* /tmp/broken/ 2>/dev/null
    rm -f /data/portal.sqlite /data/portal.sqlite-wal /data/portal.sqlite-shm
    cp -a /backups/<timestamp>/portal.sqlite /data/   # decrypt first if it is portal.sqlite.age
    cp -a /backups/<timestamp>/maps /data/ 2>/dev/null || true
    chown -R 1000:1000 /data
  '

docker compose up -d portal
curl -fsS localhost:5180/health?deep=1
```

If running under a plain bind mount instead (`DATA_DIR=/var/lib/cbm`, systemd unit, no Docker):

```bash
systemctl stop cbm-portal
mv /var/lib/cbm /var/lib/cbm.broken  # keep the bad state for diagnosis
mkdir -p /var/lib/cbm
cp -a /backups/<timestamp>/portal.sqlite /var/lib/cbm/
cp -a /backups/<timestamp>/maps         /var/lib/cbm/
systemctl start cbm-portal
curl -fsS localhost:5180/health?deep=1
```

Then check a published map's public page still serves the same file sizes as before — the published bytes are the promise, so that is the real "restore succeeded" test. There is **one writer**: stop the server before any script that writes SQLite (`seed-demo`, `import-map`, `propose-update`) and before restoring.

**Drilled for real 2026-08-09** against the live host (at that point carrying only one admin user,
so a safe moment to actually destroy and restore rather than merely rehearse): stopped the portal,
deleted `portal.sqlite` from the running volume, restored from that day's `docker compose run --rm
backup` snapshot, restarted, and `/health?deep=1`'s `users: 1` confirmed the admin account survived
the round trip.

### 5b. A data subject's request — what you hold, and erasing it

`application` and `message` hold names, email addresses, phone numbers and free text that members of the public typed into the apply and contact forms. Since 2026-08-25 they have a retention window and an erasure path (`technical-audit_2026-08-25` N8); before that date nothing ever deleted a row from either, and the privacy statement described an intention rather than a mechanism.

**Retention runs by itself.** The server purges `application` and `message` rows older than **24 months** once a day, logging what it took. An application that belongs to a **live customer is exempt** and kept as long as the account is: it is the record of how that organisation came to hold one, and the evidence behind the vetting decision. To see what is due, or to run it on demand:

```bash
node scripts/personal-data.mjs --retention
```

Run that from the repo root — `C:\Claude\community-bus-maps` on the laptop, or `docker compose exec portal node scripts/personal-data.mjs --retention` from `/opt/community-bus-maps` on the VPS. It is a dry run; `--yes` makes it delete.

**Answering a request.** Four steps, in this order:

1. **Look**, before anything else. `node scripts/personal-data.mjs --show someone@example.org` lists every application, message and user account matching that address, case-insensitively — people apply under one spelling and write in under another.
2. **Erase the form submissions.** `node scripts/personal-data.mjs --erase someone@example.org` is a dry run; add `--yes` to delete. Whole rows go, not blanked fields: a record with the name removed and the free text left is usually still personal data, and half an erasure is harder to defend than a whole one.
3. **Handle the account separately, if there is one.** The script deliberately does not touch `user`, because deleting that row orphans the audit trail — who published which map, who approved which organisation — which the service is obliged to keep. Instead: set the user to `disabled` in **admin → Users**, revoke their sessions with **admin → Users → revoke all sessions**, and if the name itself must go, replace it with `Former user #<id>`. The audit rows keep pointing at an id that no longer names anybody.
4. **Deal with the backups, and write down a date.** This is the step that is easy to skip and the one that makes the erasure real: a restore from any backup taken before today brings the person back. Nothing rewrites a backup in place — the encrypted ones cannot be edited at all — so the erasure completes when the last copy holding them rotates out: **14 days** on the VPS, **90 days** on the laptop mirror. Both of those are now genuinely days — check that before you write the date down, because the VPS half was **14 folders** until 2026-08-28 and on the morning it was found that came to 25 hours, not a fortnight. A date computed off it any time before then was wrong in the person's favour, but wrong. Record that date, and the request itself, in `community-bus-maps-ops/P3-incident-log.md`. That log is outside the database the person asked to be removed from, which is the only place a record of the removal can honestly live.

## 6. Housekeeping

```bash
npm run prune:staged -- --days 90             # dry run by default; add --yes to delete
```

Removes staged payloads of **settled** monthly refreshes and the data an accepted refresh replaced, once they are older than `--days`. It never touches a pending update, a map's live data, or any rendered version. The Ops tab shows how much it would free.

Sessions expire themselves (the server purges hourly). Nothing else grows unbounded except renders, which are kept on purpose — every version stays downloadable.

**Who has used the portal, and what they did.** `scripts/activity-report.mjs` prints one block per person for the last 28 days: their account and organisation, the sign-in links they were sent and used, every change the audit log holds for them with the maps it touched, and any application or message they sent. Command-line actors such as `cli:delete-map` are listed apart. It reads the database read-only and prints real names and addresses, so read it and do not paste it into a file under git. It counts **used sign-in links, not sessions**, because the hourly purge above deletes a session once it expires, and it cannot see page views: *no actions* means nothing was changed, not that nothing was looked at. From the laptop, from any folder, with no placeholders:

```bash
npm --prefix "C:/Claude/community-bus-maps" run ssh -- "docker compose exec -T portal node scripts/activity-report.mjs"
```

Add `--days 90` inside the quoted command for a longer window (any whole number of days), or `--json` for the same figures as JSON. Run on the laptop instead, as `npm run activity:report` from `C:\Claude\community-bus-maps`, it reports the local portal, whose seeded demo accounts read like real customers.

## 7. Upgrading the app

**Two things about the image changed on 2026-08-20 and both bite silently if missed.**

The Dockerfile's `FROM` is now **pinned by digest** (`node:24-slim@sha256:3638d9a6…`), because it said "pinned by digest-able tag" above a floating tag and a rebuild months apart could move the very bytes the product guarantees — which is not hypothetical, it is what the Liberation Mono incident was (`technical-audit_2026-08-19` V6). Bump it from Dependabot's monthly `docker` PR or with `docker buildx imagetools inspect node:24-slim`, run from anywhere; record the new digest in `CHANGELOG.md`, and re-run `npm run verify` before deploying, because a base-image change is a rasteriser change until proved otherwise.

**After any rasteriser change — a `sharp` bump or a base-image bump — check the sheets you have already published**, on the host, where the bytes actually live:

```bash
npm run ssh -- "cd /opt/community-bus-maps && docker compose run --rm portal node scripts/rerasterize-stored.mjs --check"
```

Run from the repo root on the laptop (`C:\Claude\community-bus-maps`); `/opt/community-bus-maps` is `DEPLOY_APP_DIR` on the host. It writes nothing — it re-rasterises each stored SVG to a scratch file and reports whether the bytes would change. If any would, **look at one of the changed sheets before doing anything else**: the Liberation Mono incident moved bytes too, and every sheet was wrong. Then `--apply` to bring the stored files back in line, and record it in `CHANGELOG.md`.

**Run for `sharp` `0.34.5` → `0.35.3` on 2026-08-22, against the real store: 152 stored JPGs, 0 would change, 0 failed.** That closes the question the bump left open — it had been shown to move no bytes on Windows, which is not the same claim as no bytes on `node:24-slim`, and until this run nobody had asked the host. No `--apply` was needed and no re-baseline follows.

**The rule when byte continuity and a security patch pull apart: the patch wins.** `sharp` carried a high-severity advisory for weeks on the grounds that the bytes are a product guarantee. A re-baseline is a normal, announced, recoverable event; an unpatched image parser in production is not. (In the event, `0.34.5 → 0.35.3` moved no bytes at all — but that was the outcome, not the reason.)

The image also copies a new top-level **`views/`** directory holding the signed-in app's HTML shells, which moved out of `public/` so `@fastify/static` can no longer serve them (S7 — `/app/admin.html` returned 200 to anyone while `/app/admin` correctly redirected). Miss that `COPY` line and every `/app` page 404s while the public site looks perfectly fine.

1. `npm ci` (respect the lockfile — see the sharp warning below), then **`npm test`**.
2. **`npm run verify`** with `FIXTURE_DIR` + `PLACE_FIXTURE_DIR` pointing at the private fixtures. This is the release gate: it re-renders the area map, the place map **and the two expert styles** and requires them to be byte-identical.
3. Deploy, then `/health?deep=1`.

**The sharp/libvips warning is not boilerplate.** The print JPG is the product; a different libvips build can encode the same SVG to different bytes, which would silently break "the file we serve is the file that was approved". Treat any `sharp` bump as a change that must pass step 2 on the target platform before it ships.

## 8. Licensing gate

`docs/LICENSING.md` lists the data sources and what must be credited (the **bustimes.org terms** question is resolved — see LICENSING.md §3). It is a launch go/no-go, not a footnote: the review lines at the bottom of that file are meant to be filled in before the public site is announced.

## 9. The live host, as built (2026-08-09)

Everything above is the general recipe. This is the as-built record of the one host that actually exists, for picking this up cold — the VPS and DNS decisions are recorded in the go-live record, now history at [`_archive/GO-LIVE.md`](_archive/GO-LIVE.md); this is just "how do I get back in and what state is it in".

- **Host:** OVHcloud VPS, Ubuntu 26.04, IP + access details in `community-bus-maps-ops/OVHCloud settings.txt` (outside this repo — operator-only). User `ubuntu`, password auth and root login disabled.
- **SSH key:** a dedicated keypair at `~/.ssh/busmaps_vps` on the laptop (not the default identity — pass `-i` explicitly, or rely on `.env`'s `DEPLOY_SSH_KEY` for `scripts/deliver-map.mjs`).
- **App directory:** `/opt/community-bus-maps`, a plain `git clone` (not a deploy artifact) — `git pull && export GIT_SHA=$(git rev-parse --short HEAD) BUILT_AT=$(date -u +%FT%TZ) && docker compose build portal && docker compose up -d portal` is the whole upgrade.
- **GitHub access from the host:** a dedicated, read-only Deploy Key (`~/.ssh/id_ed25519_deploy` on the VPS, registered against this GitHub repo only — see repo Settings → Deploy keys). Not the laptop's key, and not write-capable.
- **Firewall:** `ufw` allows only 22/80/443.
- **Reverse proxy:** Caddy (`Caddyfile` in this repo, deployed to `/etc/caddy/Caddyfile`), serving https://busmaps.uk. Carries the security headers and the CSP since 2026-08-19 — **deployed by `npm run deploy:caddy`, not by `npm run deploy`**: see §3a.
- **Backups:** host cron (`crontab -l` as `ubuntu`) runs `docker compose run --rm backup` daily at 03:15. A Windows scheduled task on the laptop (`BusMaps-PullBackups`, `schtasks /Query`) runs `C:\Claude\community-bus-maps-ops\pull-backups.ps1` **hourly** (since 2026-08-30; before that daily at 08:00), through the hidden launcher `run-pull-hidden.vbs` and without waking the machine, to pull them off-box into `community-bus-maps-ops\backups\` (that folder is local-only, not this repo, not synced anywhere public — see that folder's own notes, `P5-monitoring-and-alerting.md`).
- **Content:** fresh database, one operator organisation (*BusMaps.uk (pilot)*), all 8 built towns + 5 built places imported via `scripts/deliver-map.mjs` and published via `scripts/publish-baseline.mjs` — see [`_archive/GO-LIVE.md`](_archive/GO-LIVE.md) §3 "Content on the live site". That was the state at go-live; the first customer followed in September 2026.
- **Deploy history:** `da66dc9` (2026-08-11, `0.9.2-pilot`) → `7603d39` (2026-08-12, `0.9.3-pilot`, both update-flow tranches) → `5e20950` (2026-08-12, admin org-reassignment, #28) → `59f3a17` (2026-08-12, `0.9.3-pilot`, H9/#30, P8a rebuild/#32, `delete-map.mjs`/#33) → `e91b68c` (2026-08-13, #35 service-list link, #36 admin nav link) → `3254c02` (2026-08-13, **`fontconfig` fix — see [`_archive/CHANGELOG-to-2026-08-19.md`](_archive/CHANGELOG-to-2026-08-19.md), "the live host rendered every sheet in monospace"**, #37, then `scripts/rerasterize-stored.mjs --apply` run against all 13 maps/60 stored sheets to re-encode existing JPGs, same commit) → **`fc68a4c`** (2026-08-18, `0.9.4-pilot`, public text follows current bus policy instead of the 2021 strategy — BSIP funding claim replaced with LABG/EP-scheme framing, two new FAQ entries, #40). The box then ran ahead of this list unrecorded as far as **`fa4d6da`** (PR #44, sample refresh) — found on 2026-08-19 by reading the host rather than this document, which is exactly the case the sentence below is about. → **`e3e2339`** (2026-08-19, `0.9.4-pilot`, the P1 round of Peter's 32-comment map review: engine re-vendor, `Octolinear schematic`→`Simplified street map` and `nearby towns`→`nearby places`, and the per-state sheet-version stamp — #48, #49, carrying the undeployed #45/#46/#47 docs commits with it). Backup taken immediately before (`backups/2026-08-19T09-49-29`, 13 maps, 161.6 MB). Post-deploy `/health?deep=1`: `status: ok`, `gitSha: e3e2339`, `builtAt: 2026-08-19T09:49:56Z`, all four checks green (`sharp 0.34.5` / `vips 8.17.3`). Verified beyond the probe that the three published St Ives sheets still download HTTP 200 and that no `-draft` sidecars were created — every render predating this deploy carries no version line, and `draftStamp` is written to decline those rather than rewrite them. → **`fa0f3c0`** (2026-08-20, `0.9.4-pilot`, the P0 block of `technical-audit_2026-08-19` — security headers and a strict CSP, `npm test` and a dependency audit in CI, `deliver-map.mjs`'s health check made to fail, `trustProxy: 1`, `PRAGMA foreign_keys`, and `@fastify/static` `8.3.0` → `10.1.3` closing four highs — #56). Post-deploy `/health?deep=1`: `status: ok`, `gitSha: fa0f3c0`, `builtAt: 2026-08-20T02:07:25Z`, all four checks green. **This deploy is also the worked example of why §3a exists:** `npm run deploy` shipped the app and the `Caddyfile` stayed behind, so the security headers were merged, the deploy was reported done, and every live response still carried none of them. Nothing failed and nothing said so — it was found by reading `curl -sI`, not by any error. The `Caddyfile` was deployed separately about half an hour later and the headers verified present on `/` and on `/m/<slug>`. `npm run deploy:caddy` now does copy-validate-reload-verify in one command so the two halves cannot drift apart again. Verified beyond the header check, because headers arriving and the CSP not breaking pages are different questions: the homepage, `/maps` (26 map links, 13 thumbnails loading, so `img-src 'self'` covers the API-served images), a published `/m/<slug>` (SVG injected live, 122 text nodes, 951 path elements), `/contact.html?kind=issue` and `/app/login.html?error=expired` all render correctly with clean consoles — the last two being the pages whose inline scripts were moved out so `script-src` could stay strict. The CSP was confirmed *enforcing* in production, not merely present: an injected inline script did not execute and an external image was blocked. The signed-in `/app` was **not** checked live — that needs Peter's own session. `/health?deep=1` is the record of what is actually on the box — trust it over any document, including this one. → **`b0c94ee`** (2026-08-22, `0.9.4-pilot`, nav current-page marking and the Pricing accessibility line — #72). Verified live by the §3a method rather than by the probe, and this deploy is why that distinction matters: `public/js/nav-current.js` returned **404 before the deploy and 200 after**, it is referenced by all four page kinds checked (`/`, `/pricing.html`, `/maps`, `/faq.html`), the `aria-current` rules are present in the served `styles.css`, and the live page sets `aria-current="page"` on exactly the right link with a distinct colour plus a 2px `#5a8dff` underline. **`/health?deep=1` proved none of that and structurally could not have** — see the caution below. → **`709a9b5`** (2026-08-27, **`0.10.0-pilot`**, the sheet tabs restyled to read as buttons and each given a description — #125). Backup taken immediately before by `npm run deploy` step 1 (`backups/2026-08-27T19-03-36`, 18 maps). Post-deploy `/health?deep=1` **with a token**, so the fields the caution below is about were actually present: `status: ok`, `version: 0.10.0-pilot`, `gitSha: 709a9b5`, `builtAt: 2026-08-27T19:03:41Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), and `npm run smoke:signin` accepted. Verified by the §3a method as well, because this change is entirely CSS and markup and the probe cannot see either: the served `/css/styles.css` now carries `--tab-lift` (3 occurrences, absent before), `/m/huntingdon` serves the new `#sheetHint` element, `/api/public/maps/huntingdon` returns a `hint` on all three outputs, and a headless-Chrome screenshot of that live page shows three tabs reading as buttons with the chosen one filled solid and the description line beneath. No `Caddyfile` change in this release, so §3a's second half does not apply. → **`683f766`** and **`4cb7722`** (both 2026-09-04, `0.10.0-pilot`: the OA-229 wrap and POI-defaults round with its engine re-vendor, #224; then the document checkers' move to the public skills repository, #226) — both deployed and verified by their own sessions and recorded here after the fact, which is the sentence above happening again. → **`e83fcdd`** (2026-09-04 22:02, `0.10.0-pilot`, the engine re-vendored with `design.laneRibbon`, the opt-in ribbon-cable lane order of OA-176 4.21 — #227, byte-inert on every stored sheet). Backup taken by `npm run deploy` step 1. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: e83fcdd`, `builtAt: 2026-09-04T22:02:14Z`, all five checks green, `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+e83fcdd` at the public edge and `check:live-routes` 93/93. Then `track-engine.mjs --apply` on the host brought 18 stored packs forward, read back 46 current / 0 behind — no stored sheet changed, as that script's header says; the first sheet to carry the key is Beaconsfield Simpson Centre v1.29, delivered as proposed update #145 (after #228's dated S6 deferral) and awaiting the customer. No `Caddyfile` change. → **`11d5f0b`** (2026-09-05 02:51, `0.10.0-pilot`, the engine re-vendored with the second half of `design.laneRibbon`, a route's own out-and-back drawn once, OA-176 4.24 — #230, byte-inert on every stored sheet). Backup taken by `npm run deploy` step 1. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 11d5f0b`, `builtAt: 2026-09-05T02:51:23Z`, all five checks green, `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+11d5f0b` at the public edge and `check:live-routes` 93/93. Then `track-engine.mjs --apply` on the host brought 18 stored packs forward and read back 46 current / 0 behind. Beaconsfield Simpson Centre v1.30 was then delivered as proposed update #146 for map #9, superseding the still-pending #145 (v1.29); pre-flight byte-identical on both sheets, customer emailed, awaiting acceptance. No `Caddyfile` change. → **`a8114e7`** (2026-09-05 05:15, `0.10.0-pilot`, the engine re-vendored with a `style` on an `internalCorridors` family — two different routes' shared section drawn once, OA-176 4.24's second half — #232, byte-inert on every stored sheet). Backup taken by `npm run deploy` step 1 (`backups/2026-09-05T05-15-09`, 18 maps, 617.5 MB). Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: a8114e7`, `builtAt: 2026-09-05T05:15:17Z`, all five checks green, `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+a8114e7` at the public edge and `check:live-routes` 93/93. Then `track-engine.mjs --apply` on the host brought 18 stored packs forward (28 already current). **The deploy was forced by a delivery that the host correctly refused first**: Ramsey v3.7 carries the new `gen_internal.js` in its payload, the host's vendored `complexity_ladder.js` was still `11d5f0b`'s, and the pre-flight threw on the missing `style` map before the live service was touched — the generator failing loudly against an older module rather than drawing the 305 in the 303's colour. After the deploy the same payload was delivered as proposed update #147 for Ramsey (map #6), pre-flight byte-identical on all three sheets, customer emailed, awaiting acceptance. No `Caddyfile` change. → **`cce6ab9`** (2026-09-05 10:00, `0.10.0-pilot`, the review screen reads the complexity band that every area pack already carried — buses-data OA-088, #237). Backup taken by `npm run deploy` step 1 (`backups/2026-09-05T10-00-40`). Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: cce6ab9`, `builtAt: 2026-09-05T10:00:46Z`, all five checks green, `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+cce6ab9` at the public edge and `check:live-routes` 93/93. **No engine change, so no re-vendor and no `track-engine.mjs`** — the packs were already carrying `complexity.json`, and what shipped is something that finally reads it. Confirmed by the §3a method rather than by the probe, which is the right test for a change that is a reader and a screen: the live `/app/review.js` carries seven `complexity` sites and is byte-identical to the merged commit's, where the previous build had none. The live store was then read directly — the reader could not be invoked inside the container from the laptop, so this is the data it will be handed rather than its output: **all 8 AREA packs hold a `complexity.json` carrying a band the reader recognises** — St Ives GREEN, Beaconsfield AMBER (`K5=0.54>0.5`), High Wycombe GREEN, Huntingdon AMBER (`P=64>42`), March GREEN, Ramsey GREEN, St Neots GREEN, and **Wisbech RED (`D5=3.7>3.5`), published** — while all 10 PLACE packs hold none, which is the deliberate silence and not a gap. Merged on a base three commits old (#234, #235, #236 had landed since its checks ran) and the **post-merge** run on `cce6ab9` read green on all three workflows before the deploy, which is the only run that tested what shipped. No `Caddyfile` change. → **`58e2023`** (2026-09-05 11:10, `0.10.0-pilot`, the landmark answer can leave the portal: `GET /api/maps/:id/poi-tiers`, the read-only operator token's third route — buses-data OA-233, #239). Backup taken by `npm run deploy` step 1. The post-merge run on `58e2023` read green on all three workflows before the deploy. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 58e2023`, `builtAt: 2026-09-05T11:10:19Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+58e2023` at the public edge and `check:live-routes` **94/94** — one more than the 93 every entry above records, and the one more is this route. Confirmed by the §3a method, which for a new route means asking it: anonymously it answers 401 `Please sign in.`; with `OPERATOR_TOKEN` it answers 200 for map 3 (High Wycombe: 171 answered, 171 saved in the portal, 0 in the delivered pack, 6 `must` / 139 `may` / 26 `miss`); the neighbouring `/landmarks` still refuses the token. **No engine change, so no re-vendor and no `track-engine.mjs`.** The first real round trip then ran from the laptop — `poi_tiers_sync.js --town "High Wycombe"` (claude-skills) — and found the portal's answer had moved on since the 2026-09-03 paste (North Lawn promoted to `must` and renamed) and that the bus-work worklist, which compares every area map on every run, raised a second town nobody knew about: St Ives holds three answered places in the portal that its source has never had. No `Caddyfile` change. → **`07d8b0a`** (2026-09-06 02:19, `0.10.0-pilot`, the landmark chooser stops selecting its own street names and its progress chips carry their counts — buses-data OA-252, #243). Backup taken by `npm run deploy` step 1. The post-merge run on `07d8b0a` read green on all three workflows before the deploy. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 07d8b0a`, `builtAt: 2026-09-06T02:19:16Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+07d8b0a` at the public edge and `check:live-routes` 94/94. **No engine change, so no re-vendor and no `track-engine.mjs`** — the three files are the chooser's own client assets. Confirmed by the §3a method, which is the only test that could see this: the live `/app/app.css` carries `-webkit-user-select: none; user-select: none;` inside the `.lm-stage svg` block, where the previous build had neither, plus five `.lm-n` chip-badge rules; the live `/app/landmarks.js` carries seven `paintFilterCounts` / `isAnswered` sites where the previous build had none. The fault itself was measured in a browser before the fix and after it, with the rule toggled off and on as its own control — a `preventDefault()` on `dblclick` cannot stop a word selection Chrome makes on the second `mousedown`, and the same probe found the half nobody had reported, which is that a PAN DRAG was selecting road names too. No `Caddyfile` change. → **`Caddyfile` only, no app deploy** (2026-09-06 03:38, buses-data OA-086 phase 1, #246 merged as `51f44d8`). **The first entry in this list that is a `Caddyfile` deploy on its own** — every other one is an app deploy that either did or did not carry a `Caddyfile` change, which is precisely the asymmetry §3a exists to name: `npm run deploy` cannot ship this file and `npm run deploy:caddy` cannot ship the app. It went up from a WORKTREE of `main`, because the laptop's checkout was on another session's branch whose `Caddyfile` predates the merge — deploying from the checkout you happen to be sitting in is the same class of mistake as not deploying at all. `sudo caddy validate` accepted it and the reload ran; `npm run deploy:caddy`'s own read-back found all five security headers present and www still answering `308`. **The two things that script cannot see were then read on the host, before and after, which is what makes this an observation rather than a claim.** Before: `remote_ip` and `client_ip` both held the full address `2a0a:ef40:9fa:3e01:88b8:a521:17ea:23f8`. After the same request: **both** read `2a0a:ef40::`, the /32 mask, and `uri` still read `/maps?q=REDACTED` — so the OA-006 filter survived the edit, which is the regression this file's own instructions are written to catch. The retention half shows up in no log line, so it was read out of the RUNNING config rather than inferred from `validate` accepting it: `curl -s localhost:2019/config/logging/logs/log0/writer/` on the host returns `roll_size_mb: 10, roll_keep: 10, roll_keep_days: 30` — the 30 days `/legal.html` promises the public. **Half of phase 1 is still not live and that is expected**: `X-App-Version` remains `0.10.0-pilot+07d8b0a`, so the app's own request log is still writing full addresses and will mask them from the next ordinary `npm run deploy`. No backup was taken and none applies — nothing here touches the database, the store or the container. → **`28f7370`** (2026-09-06 03:41, `0.10.0-pilot`, a monthly update now names the places OpenStreetMap has gained and lost — buses-data OA-253, #247, carrying #245 and #246 with it). Backup taken by `npm run deploy` step 1. **`main` had moved two PRs since #247 was cut**, so the PR's own green checks were about an older base; the post-merge run on `28f7370` read green on all three workflows before the deploy, which is the only run that tested what shipped. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 28f7370`, `builtAt: 2026-09-06T03:41:37Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+28f7370` at the public edge and `check:live-routes` 94/94. **No engine change, so no re-vendor and no `track-engine.mjs`.** Confirmed in both halves, because neither one alone would have done. The CLIENT half by the §3a method: the live `/app/changes.js` carries `landmarkBullets` and the sentence it builds, the live `/app/editor.js` renders through `PC().landmarkBullets` and holds no second copy of the wording. The SERVER half **cannot be curled at all** — it runs only when an update is staged — so it was exercised inside the running container against real map data: map 1 against itself answered `known, 0 added, 0 removed, unchanged` (the quiet case), and map 1 against map 2 answered `known, 25 added, 30 removed`, naming real places. A first attempt pointed at a `proposed/1/` folder that exists on the laptop's dev volume and not on the host answered `landmarksKnown:false` — the silence guard doing exactly its job on an input that was nonsense, which is worth recording because for a minute it looked like a feature deployed dead. **It also carried the app half of OA-086 phase 1 live**, three minutes after the entry above said it would go with the next ordinary `npm run deploy` — read back rather than assumed: the container's own request log now writes `"remoteAddress":"127.0.0.0"` and `"172.18.0.0"` where it wrote the full address before. No `Caddyfile` change. → **`a9b1ab7`** (2026-09-06 06:31, `0.10.0-pilot`, the FAQ answers *a landmark is missing* in the order the question resolves — chooser, category, then OpenStreetMap with two links — buses-data OA-261, #252). Backup taken by `npm run deploy` step 1 (`backups/2026-09-06T06-31-18`). The PR was cut from the then-current `main` (`5fe2ef4`) and the post-merge run on `a9b1ab7` read green on all three workflows before the deploy. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: a9b1ab7`, `builtAt: 2026-09-06T06:31:24Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+a9b1ab7` at the public edge and `check:live-routes` 94/94. **No engine change, so no re-vendor and no `track-engine.mjs`** — the change is one static page and a changelog fragment. Confirmed by the §3a method, which is the only test that can see a static page: the live `/faq.html` carries `id="landmarks"` and both OpenStreetMap links, the Beginners' Guide and Notes, where the previous build had neither. No `Caddyfile` change. → **`0428bca`** (2026-09-07 02:10, `0.10.0-pilot`, the competition goes on the page: opportunity.html gains a `#who-else` card and loses its *small fraction of what a design agency charges* claim, and pricing.html replaces *order of magnitude below* and *the alternative is nothing* with the public-record figures — a market-town schematic £970, updating £88 an hour — buses-data OA-264, #254). Backup taken by `npm run deploy` step 1 (`backups/2026-09-07T02-10-37`, 18 maps, 637 MB). The PR was cut from the then-current `main` (`e78bdf0`) and the post-merge run on `0428bca` read green on all three workflows before the deploy. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 0428bca`, `builtAt: 2026-09-07T02:10:44Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+0428bca` at the public edge and `check:live-routes` 94/94. **No engine change, so no re-vendor and no `track-engine.mjs`** — two static pages and a changelog fragment. Confirmed by the §3a method with a before-and-after read fifty seconds apart: at 02:10:29Z the live `opportunity.html` and `pricing.html` carried no `id="who-else"` and no `£970` under `X-App-Version …+a9b1ab7`; at 02:11:20Z both carried `£970` and `£6,600`, the opportunity page carried the `who-else` anchor, and neither carried the sentence it lost. **One thing that reads as a fault and is not:** the three procurement links in the new card answer `403` to `curl`, because Contracts Finder and Find a Tender sit behind bot protection; the same notices opened in full in a browser on 2026-09-06, and the fourth link, Human Transit, answers `200`. Do not "fix" those links. No `Caddyfile` change. → **`cd7e715`** (2026-09-07 04:00, `0.10.0-pilot`, the diagram engine re-vendored so a linear feature's OSM ways are stitched into chains before they are mapped — buses-data OA-059, #255). Backup taken by `npm run deploy` step 1. **`main` was standing RED on `verify` before this merge and that was the ordering asymmetry rather than a fault**: `d07e186` ran the OLD vendored engine against a `buses-data` `main` that already carried the refreshed area fixture, so the byte gate correctly reported 303,237 B against 302,440 B. The post-merge run on `cd7e715` read green on all three workflows, and clearing that red is what this deploy was for. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: cd7e715`, `builtAt: 2026-09-07T04:00:20Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted; `X-App-Version: 0.10.0-pilot+cd7e715` at the public edge and `check:live-routes` 94/94. **`track-engine.mjs` on the host reported `46 already current, 0 BEHIND, 0 skipped` and needed no `--apply`, and that is an ANSWER rather than a step missed**: the two files that changed are `engine/expert/diagram_internal.js` and `engine/linear_features.js`, and a module is resolved out of `engine/` through `SKILL_ASSETS` rather than copied into a data pack, so the deploy itself reached every map. §4a is satisfied by running it on the host and reading that number, which is the whole point of that section. Confirmed by the §3a method, and for an engine change that means reading the bytes the running build actually holds rather than curling a page: inside the container `engine/linear_features.js` carries `A.slice(1).reverse().concat(B), A.length-1` — the hairpin fix Peter found by eye on the Beaconsfield diagram — and exports `stitchSegs`, and `engine/expert/diagram_internal.js` names `stitchSegs` three times, none of which the previous build had. **Nothing published changed byte-for-byte**; what changes is the next render of each map. No `Caddyfile` change. → **`75baa5a`** (2026-09-07 13:42, `0.10.0-pilot`) — **the last entry in this list written after the fact**, and the one that made the case for §3b above. `75baa5a` is the merge of #257, whose entire content is the entry for `cd7e715` immediately before it, so **this deploy shipped no application change whatever**: it exists solely to move the live site onto a commit that recorded a previous deploy, which is the loop the new ordering closes. It was made 9 hours into a 12-hour grace, about two and a half hours before the drift would have turned the next push in any of the three repositories red. Post-deploy `/health?deep=1` with a token: `status: ok`, `gitSha: 75baa5a`, `builtAt: 2026-09-07T13:42:43Z`, all five checks green (`sharp 0.35.3` / `vips 8.18.3`, email `provider: resend`), `smoke:signin` accepted by the provider; `X-App-Version: 0.10.0-pilot+75baa5a` at the public edge and `check:live-routes` **94/94** (19 public, 57 guarded, 9 page, 9 reaches). **No engine change, so no re-vendor and no `track-engine.mjs`**, and no `Caddyfile` change. **There is nothing to confirm by the §3a method and that is the honest answer rather than a check skipped**: no file served to anybody differs from the previous build, so the version stamp is the only observable difference there is — which is exactly what makes the drift this closed a fact about the repository rather than about the site. → **the ordering change itself** (2026-09-07, `0.10.0-pilot`, §3b above and this entry — #258, buses-data OA-266 retired as `54dde1a`). **The first entry in this list written BEFORE its deploy, which is the whole point of it**, so it names a pull request rather than a sha: the commit actually deployed is the merge of #258, and that is the relation §3b describes. No application change — `docs/` only — so no engine change, no re-vendor, no `track-engine.mjs`, no `Caddyfile` change, and **nothing for the §3a method to see**, for the same reason as the entry before it. What to read back afterwards is therefore only that the deploy took: `/health?deep=1` with a token should carry the merge sha, and `X-App-Version` at the public edge should agree with it. If it does, nothing more is written here — that is §3b's rule about post-deploy readings, applying to itself on its first outing. → **the tube-map diagram parked** (2026-09-10, `0.10.0-pilot`, #263, buses-data OA-297 — the change is `ea1536f`; the commit deployed is the merge of #263, and this entry was written before the deploy per §3b). An application change with **no engine change, no re-vendor and no Caddyfile change**: `engine/expert/` is untouched and `TUBE_DIAGRAM` is unset on the host, so nothing is configured at deploy time. What to read back afterwards: `/health?deep=1` carries the merge sha; `GET /api/public/maps` lists no `internal_diagram` on any map; the three St Ives diagram URLs (`inline/internal-diagram`, `preview/internal-diagram`, `internal-diagram.svg`) answer 400 where they answered 200 on the morning of 2026-09-10; and the St Ives editor, as a customer, shows three output rows and no *Ask us* button. → **four outputs, not five** (2026-09-10, `0.10.0-pilot`, #264, the two counts #263 missed on the examples page and the homepage caption; the commit deployed is the merge of #264, entry written before the deploy per §3b). Copy only — nothing to configure, nothing for §3a; read back that `/health?deep=1` carries the merge sha and that `/examples.html` says *A fourth sheet*. → **the national bus-map directory in the /maps place search** (2026-09-11, `0.10.0-pilot`, #265, buses-data OA-308 tier 2 — the change is `ad778e2`; the commit deployed is the merge of the pull request carrying THIS entry, and this entry was written before the deploy per §3b). An application change with **no engine change, no re-vendor and no `Caddyfile` change**, and nothing to configure: the panel reads a file that ships inside the image. What goes out with it is #266, the documentation fix that six pages still called this repository private. **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha; `X-App-Version` at the public edge agrees with it; `check:live-routes` should read the same count as the entries above, because this adds no route — the panel is rendered into `/maps`, which already exists. The §3a check is the one that can see this and the probe cannot: `https://busmaps.uk/maps?q=York` must carry the *Not ours* panel naming **City of York Council** (the enquiry the whole directory was built for), and `https://busmaps.uk/maps?q=Swavesey` must carry the honest miss — *we cannot tell you which authority covers Swavesey* — rather than a guessed county. `curl -s "https://busmaps.uk/data/bus-map-directory.json" | head -c 200` should show the projection’s own `_comment`, which is how to tell at a glance that the vendored file is the field whitelist and not a copy of the private survey. → **a street is not a place** (2026-09-11, `0.10.0-pilot`, #269, buses-data OA-311 — the change is `5c2be90`; the commit deployed is the merge of the pull request carrying THIS entry, and this entry was written before the deploy per §3b). **The first release in a while that takes something AWAY from what a reader already sees**, so the read-back matters more than usual and a probe can see none of it. Searching `/maps` for *York* returned three Buckinghamshire sheets because route 104 passes along York Road (UB8); after this, a stop or landmark name shaped like a thoroughfare answers only to its whole name. **No engine change, no re-vendor, no `Caddyfile` change and nothing to configure** — it is one reader, `src/search/index.js`, and it reaches all 137 existing `places.json` sidecars without a republish, which is exactly why it went there rather than into `place-index.js`. What to read back afterwards: `/health?deep=1` with a token carries the merge sha and `X-App-Version` agrees; **`/api/public/search?q=York` and `?q=London` return NONE of our maps**, where before they returned three each; and — the half that would otherwise go unnoticed if the rule were too greedy — **`?q=Hemingford` must still return the St Ives-area sheets**, and `?q=Bar Hill` the maps that pass through the village. A rule that silenced those as well would look like a success from the York query alone. → **ask them for one, and count who asked** (2026-09-11, `0.10.0-pilot`, #272, buses-data OA-308 tiers 3 and 4 — the change is `d5a9922`; the commit deployed is the merge of the pull request carrying THIS entry, and this entry was written before the deploy per §3b). **The first entry in this list that changes the DATABASE**, so read that half back rather than only the pages: schema version goes 2 → 3 and two new tables appear, `search_demand` and `search_demand_skipped`. They are created by `schema.sql`'s `CREATE TABLE IF NOT EXISTS` at boot, so there is no migration step to run and nothing to configure — and they are ADDITIVE in the strongest sense the rollback promise has, because a v2 release opens a v3 database and simply never selects from them. **No engine change, no re-vendor, no `track-engine.mjs`, no `Caddyfile` change.** What ships: the `/maps` directory panel gains a ready-to-send letter on every row where nobody publishes a map of the reader's town, and a submitted search matching no map of ours adds one to a tally of place names. **What to read back afterwards.** `/health?deep=1` with a token carries the merge sha and `X-App-Version` at the public edge agrees with it; `check:live-routes` reads the SAME count as the entries above, because this adds no route — `intent=submit` is a parameter on `/api/public/search`, which already exists. The §3a method is the only thing that can see the rest. `https://busmaps.uk/maps?q=Derbyshire` must carry the `dir-ask` block, and inside it the words *Put it in your own words*, *You send this, not us* and *Dear Sir or Madam* — and **no `mailto:` anywhere on the page**, which is the rule the whole letter is built around and the one thing here that would be a real fault rather than a cosmetic one. `https://busmaps.uk/maps?q=Colchester` must carry the directory card and **NO** letter, because the directory already records a map of that town; a letter there would mean the town rule is not reaching the live render. `https://busmaps.uk/legal.html` must carry *We do not keep a log of what you search for*, because the tally must not go live ahead of the page that declares it. And the database half, inside the container: `search_demand` must exist with exactly the four columns `q, n, first_seen, last_seen` — **no `created_at`, no address, no session** — and `node scripts/demand-report.mjs` must run and print a tally, empty or otherwise. A backup is taken by `npm run deploy` step 1 as always, and on this release it is the one that matters: it is the last image of the store before the schema moves. → **the local adviser's seat** (2026-09-12, `0.10.0-pilot`, #274, buses-data OA-154 Phase D1 — the change is `64f589e`; the commit deployed is the merge of the pull request carrying THIS entry, and this entry was written before the deploy per §3b). A fourth role, `adviser`: somebody local who has agreed to look at one map's current draft before it is published, on screen, watermarked, with **no download of any kind**, no other map, nothing about the organisation paying for the sheet, and no form — the answering half (D2) is still gated on OA-083. **No engine change, no re-vendor, no `track-engine.mjs`, no `Caddyfile` change, nothing to configure.** **It changes the DATABASE, schema 3 → 4**, the second release in this list to do so and additive in the same strong sense: one new table, `map_adviser_grant`, created by `schema.sql` at boot, and **four new triggers** installed by the migration — `user_role_valid_insert`/`_update`, which make the list of legal roles the database's own rule rather than a comment beside the column, and `user_adviser_no_customer_insert`/`_update`, which refuse an adviser a `customer_id` at all. That last pair is the whole access model rather than a nicety: `loadOwnedMap()` grants EDIT to any non-admin whose `customer_id` matches a map's and never consults `role`, so an adviser carrying one would be an editor of that organisation's entire estate. **What to read back afterwards.** `/health?deep=1` with a token carries the merge sha and `X-App-Version` at the public edge agrees with it; `check:live-routes` should read **102/102** (19 public, 64 guarded, 10 page, 9 reaches), up from the 94 every entry above records — three routes under `/api/adviser`, four under `/api/admin` and the `/app/adviser` page, and that count is itself the §3a check for eight of the nine things that shipped. **The ninth is the page, and the only anonymous reading worth making is the REFUSAL**: `https://busmaps.uk/app/adviser` must **302 to the sign-in page** rather than serve a shell, because there is no live adviser account yet and everything behind that door is invisible from outside it. **So the real read-back here is the database half, inside the container**: `map_adviser_grant` must exist with exactly `id, created_at, map_id, user_id, granted_by, note, revoked_at`; `sqlite_master` must hold all four trigger names above; and `UPDATE user SET customer_id = 1 WHERE role = 'adviser'` must **ABORT** rather than succeed — a statement that changes nothing either way on a store with no adviser in it, which is what makes it safe to run against the live database and the only way to see that the guard arrived. **THAT LAST CHECK WAS WRONG AND IS CORRECTED BY THE ENTRY BELOW**: with no adviser row it matches nothing, so a per-row trigger never fires and the statement SUCCEEDS with 0 changes, which reads as the guard being fine. The reading that works is a pure one — the trigger's own body out of `sqlite_master` — and that is what was actually done. Everything else in this entry was confirmed as written: `/app/adviser` 302s to the sign-in page, `check:live-routes` read 102/102 with exactly that split, schema version 4, the table's seven columns, all four triggers. → **a published head is not a draft** (2026-09-12, `0.10.0-pilot`, #276, the first fix to the entry above — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). **Found by asking what the seat's first real grantee would actually see, before granting anything.** A map's working head is its PUBLISHED version for as long as nobody edits it after publishing, which is true of every map on this box; the seat called that head *Draft 8.0* in bold, watermarked it `DRAFT — not published`, and told a member of the public not to pass on a sheet that is on the public site. The cause is a function that was only ever total behind a guard: `draftLabel()` fell through to `Draft` for any state it did not name, which was harmless while its one caller opened with `if (ver.review_state !== 'published')`. **No engine change, no re-vendor, no `Caddyfile` change, no database change and nothing to configure** — three assertions of "draft" in two files, plus the page copy, which now says in as many words that there is no newer draft waiting and that a rebuild will appear there before it goes out. **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, and `check:live-routes` reads **102/102** again, unchanged, because this adds no route. The §3a method cannot see this one from outside and neither can the probe — everything it changes is behind the adviser's sign-in — so **the evidence is the suite rather than the site**: `test-adviser-seat.mjs` now drives the published state with a control that puts the version back to a draft and requires all four readings to flip, and `prove-red-adviser-seat.mjs` puts the original fault back and requires the assertion that names it to object. The first grant on this box is what will confirm it in the world, and it should be made only after this is live. **Confirmed after the deploy** (`29365a8`): `X-App-Version` agreed, `check:live-routes` read 102/102 unchanged, and the live `/app/adviser.js` was compared against the merged commit's with `git diff --no-index` and found **identical** — the §3a method reaching the half of this change a probe cannot, because static assets under `/app` are not behind the sign-in hook. → **the email that expected you to expect it** (2026-09-12, `0.10.0-pilot`, #277 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). **Found the way nothing automated could find it**: Peter made a test grant to a second address of his own and read the email that arrived. It was `kind: 'invite'` — the one a customer's first editor gets when their organisation is approved, which is fine because that person is expecting it — sent to somebody who had applied for nothing, naming neither the map nor a reason, with an urgency line and a bare `info@busmaps.uk` sender. A third kind, `adviser`, now names the map in the subject, says what to do when the fifteen-minute link has gone stale (including the address to re-request with, because the re-request form is deliberately silent about whether an address exists), says a session lasts a week and slides, and gives permission to ignore it. **No engine change, no re-vendor, no `Caddyfile` change, no database change, no route change and nothing to configure** in the app. **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, and `check:live-routes` reads **102/102**, unchanged. **The §3a method cannot see this one and neither can the probe** — an email is served to nobody — so the evidence is `scripts/test-magic-link-email.mjs`, which asserts the words per kind with `invite` and `signin` as its control, plus the one reading that is real: **make a grant and look at what arrives.** One thing on the host is NOT in this deploy and should go with it: `EMAIL_FROM` is a bare address, so Gmail shows `info@busmaps.uk` with no display name. It accepts `Name <address>`, and a named sender is most of what makes an unexpected email credible. → **the sign-out that signed nobody out** (2026-09-12, `0.10.0-pilot`, #278 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). **Found by Peter using the adviser page as the adviser**: he pressed Sign out, landed on the home page, went back, and was still signed in. `views/app/adviser.html` was the only one of ten shells not loading `/js/csrf.js`, so its logout POST was refused 403 by the CSRF hook — and the handler navigated away without reading the answer, so the button behaved exactly like a working one. Somebody on a shared computer would have believed they had signed out. **No engine change, no re-vendor, no `Caddyfile` change, no database change, no route change and nothing to configure**: one script tag, one handler that now reads its own response, and the invariant widened so it cannot recur — `test-access-model.mjs` asserted *every shell loads `account-guard.js`* and left `csrf.js` beside it merely remembered, which is exactly why the new shell carried one and not the other. **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, and `check:live-routes` reads **102/102** unchanged. The §3a method reaches the script — `curl -s https://busmaps.uk/app/adviser.js` must now differ from the previous build and `/js/csrf.js` must be referenced by the shell — but the shell itself is behind the sign-in, so **the real check is to sign in as an adviser and press the button**, which is how it was found and the only thing that proves it. **Confirmed after the deploy** (`87075eb`): `X-App-Version` agreed, and the live `/app/adviser.js` carries the new failure message where the previous build had none — Peter then signed in as the adviser and pressed Sign out, which is the reading that counts. → **the sign-in page had one way off it** (2026-09-12, `0.10.0-pilot`, #279 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). `/app/login.html` ended with *Not a customer yet? Apply to join* and nothing else, which serves exactly one of the people who reach it: a local adviser will never be a customer, and anybody whose link has expired — which that page itself announces via `?error=expired` — had no route to a person at all. *Trouble signing in? Get in touch* now comes first, pointing at `/contact.html?kind=question`, which pre-selects the same way the footer's *Report an issue* link already does; the apply link stays, subordinate. The contact page needed no change, because its intro already carries the interest form. Also: the intro now says **use the address your account or invitation is under**, since `/api/auth/request` answers identically whether or not an address is registered and a typo is otherwise met with silence, and the placeholder `you@organisation.org` — which quietly tells a member of the public that the page is not for them — becomes `you@example.com`. **No engine change, no re-vendor, no `Caddyfile` change, no database change, no route change and nothing to configure**: one page's copy. **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, and `check:live-routes` reads **102/102** unchanged. **This one the §3a method CAN see in full and should**, because the sign-in page is the one app shell served to anybody: `curl -s https://busmaps.uk/app/login.html` must carry `contact.html?kind=question` and `you@example.com`, and must NOT carry `you@organisation.org`. **Confirmed after the deploy** (`8cd73ba`): `X-App-Version` agreed and the live sign-in page carries both. → **three more things the adviser seat said that were not true** (2026-09-12, `0.10.0-pilot`, #280 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). All three found by Peter granting, revoking, disabling and re-granting his own test account and reading what each step produced, and all three the same fault: **a page or an email asserting a state it had not checked.** (i) A revoked adviser was told *nobody has asked you yet*, when somebody had and then stopped; the page cannot tell the two empty states apart and now says neither. (ii) Asking a **switched-off** adviser reported success and reached nobody — `requestMagicLink()` returns null for any account that is not `active`, so no link was issued while the grant row was written and the response said `ok`; it is now refused `409` naming the remedy, and **not** silently re-enabled, because disabling may have been for cause. (iii) The response now carries `emailed`, so a provider that threw, or an instance with no provider, stops being reported as *a sign-in link is on its way*. And the adviser email's subject and opening line said *before it is published*, which is false for every map whose working head is its published version — they now say *new versions … before they go out*, with **no branch on the version's state**, because a sentence with no state in it cannot go stale. **No engine change, no re-vendor, no `Caddyfile` change, no database change, no route change and nothing to configure.** **What to read back afterwards**: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, `check:live-routes` reads **102/102** unchanged. The §3a method reaches only the client half — `curl -s https://busmaps.uk/app/adviser.js` must carry *no map for you to look at just now* — so the rest is the suite plus, as before, **the reading that counts: ask somebody and look at what arrives.** **Confirmed after the deploy** (`778c170`): `X-App-Version` agreed, `check:live-routes` 102/102, and Peter then asked his own test address, read the email, signed in, signed out and tried asking a switched-off account — all four behaved. `EMAIL_FROM` was set on the host in the same sitting and is NOT part of any deploy: it now reads `Peter Cooper - BusMaps.uk <info@busmaps.uk>`, verified with `docker compose exec -T portal printenv EMAIL_FROM` against the running container rather than by reading `.env`. → **the adviser in the operations handbook** (2026-09-12, `0.10.0-pilot`, #281 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). Documentation only: `H1-operations-handbook.md` §3 gained the fourth role, which it had not mentioned at all — who a local adviser is, that they are somebody outside this operation rather than a fourth hat, the one action that asks them, and the two operational gotchas (a switched-off account cannot be asked, and the console now says whether the email actually went). **Nothing served to anybody changes**, so there is nothing for the §3a method to see and no reason to expect any reading to move: `/health?deep=1` with a token carries the merge sha, `X-App-Version` agrees, `check:live-routes` reads **102/102**. It is deployed at all only because a documentation commit that is not deployed leaves `main` one ahead of the box and starts the drift clock — which is the loop §3b above exists to close, applying to itself again. → **the sample band asks which map** (2026-09-12, `0.10.0-pilot`, #283 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). Ships `0b13aed` (#282, buses-data OA-320): the red *PILOT — SAMPLE MAP* band said *Not published by any organisation*, which is a claim about the MAP, while the only thing gating it was the site-wide `PILOT_MODE` — so no value on any customer could stop it printing above that customer's own badge. It is now gated on `customer.is_sample` as well, with `is_demo` forcing it on. **This is the first deploy since v4 that carries a migration**, one additive column defaulting to 1, and `SCHEMA_VERSION` goes 4 → 5 in the same pull request as this entry. Migrations run at import of `src/db/index.js` before the port binds, so a healthy start with `database: ok` IS the evidence that it applied; a crash-loop is what failure looks like. **Nothing served to anybody should change**, because all 18 published maps belong to `busmaps-uk-pilot` and every one stays a sample: expect `check:live-routes` to read the same count it read for #281, `/health?deep=1` with a token to carry the merge sha and `X-App-Version` to agree. **The reading that can actually see this one is not a §3a read-back at all** — it is `docker compose run --rm portal node scripts/restamp-renders.mjs` on the host, which must report **0 would change**. That is the acceptance test and it points the unusual way: any number ABOVE zero means the change moved a sheet it should not have, and is a reason to stop rather than to carry on. The laptop's development store answered 31 inspected / 0 would change, which is a different store and not an answer about this box. → **four rounds land together** (2026-09-13, `0.10.0-pilot`, #290 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). Ships `1a863c2` (#286, buses-data OA-042), `790a70a` (#287, buses-data OA-325), `8cd06a4` (#288, buses-data OA-284) and `09234c2` (#289, buses-data OA-311). **This deploy carries NO migration and `SCHEMA_VERSION` stays at 5** — #287 adds the CHECK that the constant is bumped with a migration, which is not itself one; nothing under `src/db/` or `schema.sql` moves anywhere in this range, read as a diff rather than remembered. **No route is added or removed** either — #288 rewrites the `/o/:slug` handler and registers nothing new — so `check:live-routes` should read the same count it read for #283, and a different one is a reason to stop. **Two of the four are retired by THIS deploy and not by their merge, and both had their faults confirmed live at `4bea393` first, so a clean reading afterwards means something.** OA-042: the opportunity page is the page every email in the outreach plan sends its reader to, and it still carried the 13 August count — `curl -s https://busmaps.uk/opportunity.html` returned *Seven towns and five places* minutes before this entry was written, and must not afterwards. OA-284: `npm run check:sitemap` read **50 advertised, 1 wrong** against the live box at `4bea393` — `/o/busmaps-uk-pilot`, no `<link rel="canonical">`, the single fault in the estate — and must read 50 of 50. **The other two have no live read-back and that is stated rather than left blank.** OA-311 changes ranking, not routing: a search for *station* should stop returning the eight sheets whose only match is the stop name *Bus Station*, while typing the whole name still finds them, so the check is a search and not a probe. OA-325 is a CI gate with no user-visible surface at all; its evidence is the `test` job, and there is nothing on the running site that can see it. → **the printed sheet gets display guidance** (2026-09-13, `0.10.0-pilot`, #292 — the commit deployed is the merge of the pull request carrying THIS entry, written before the deploy per §3b). Ships `6cb02dc` (#291, buses-data OA-327): a new FAQ entry `#putting-it-up` saying how high to mount a printed sheet, whose permission to ask before fixing it to a bus shelter, and what to print it on; a standalone *If you put a printed sheet up* section in the accessibility statement; a pointer from the customer guide; and the reasoning in `docs/ACCESSIBILITY.md`. `terms.html` and `legal.html` are deliberately untouched, because this is advice and not a licence condition. **This deploy touches no code at all** — the merged commit is five files, all of them public HTML, documentation or a changelog fragment, with nothing under `src/`, `schema.sql` or any migration, read as a diff rather than remembered. So `SCHEMA_VERSION` stays at 5 and **no route is added or removed**: the FAQ entry is an anchor inside a page that already exists, so `check:live-routes` should read the same count it read for #290, and a different one is a reason to stop. **The live read-back that can see this one is a grep of two public pages, and both return nothing today.** `curl -s https://busmaps.uk/faq.html | grep putting-it-up` must return the entry's `id` afterwards, and `curl -s https://busmaps.uk/accessibility.html | grep 'If you put a printed sheet up'` must return the new section, on a page whose *last reviewed* line must also then read **13 September 2026** rather than 26 July. **Two things in this range have no live surface and that is stated rather than left blank.** The unwrapping of `docs/ACCESSIBILITY.md` is operator-facing and invisible to the site; and the re-date's own claim that the system is *a pilot with no customers* was verified BEFORE the date moved, against `https://busmaps.uk/api/public/maps`, which returned 18 published maps under exactly one organisation, *BusMaps.uk (pilot)*, which is us. → **`157ec75`** (2026-09-13, PR #293, buses-data OA-261 item 1): the non-partisan position, written once so it never has to be argued case by case. A new FAQ entry `#non-partisan` — *Is this political? Can a political party publish a map?* — saying yes, that a party, a campaign group or a councillor's office is an organisation like any other, that a sheet is drawn from BODS and OpenStreetMap by ordinary code and carries the publishing organisation's name and badge and nobody else's, that the credits-stay-on and do-not-alter conditions [`legal.html`](https://busmaps.uk/legal.html) already imposes are what make a logo added to a print a breach rather than a new rule, and that a published sheet is a public download we have no say over once it is printed. A pointer to it from *Who can apply?*, and a fourth item in `background.html`'s *what holds this together* beside the open data, the operator cross-check and the licence — that list said **Three things** and now says four. **This deploy touches no code at all**, read as a diff rather than remembered: the merged commit is three files, two public HTML pages and a changelog fragment, with nothing under `src/`, `schema.sql` or any migration. So `SCHEMA_VERSION` stays at **5** and **no route is added or removed** — the entry is an anchor inside a page that already exists, so `check:live-routes` must read the same count it read for #292, and a different one is a reason to stop. **The live read-back that can see this one is a grep of two public pages, and both return nothing today** — measured before the entry was written, which is what makes them evidence: `curl -s https://busmaps.uk/faq.html | grep non-partisan` returned **0** and `curl -s https://busmaps.uk/background.html | grep non-partisan` returned **0** at 11:30Z on 13 September. Afterwards the first must return the entry's `id` and the second the new bullet. **And one thing to know before reading the post-merge run**: #293's first local `npm test` failed `check-chrome`, `test-search` and `prove-red-search` on an unchanged tree and its second passed, which is buses-data OA-335 — those three together are the known flake, anything else is real. → **`aa498a7`** (2026-09-14, the landmark-tier control that had never run where CI looks, #298) — **written before the deploy per §3b, so the sha that gets deployed is the merge carrying this entry and not `aa498a7` itself.** **This one ships nothing the public can reach**, read as a diff rather than remembered: three files, `scripts/test-landmark-tiers.mjs`, `scripts/prove-red-landmark-tiers.mjs` and one `CHANGELOG.d/` fragment, with nothing under `src/`, `public/`, `engine/`, `schema.sql` or any migration. So `SCHEMA_VERSION` stays at **5**, no route is added or removed, and `check:live-routes` must read the same count it read for #294 — a different one is a reason to stop. **The live read-back that can see this deploy is the build stamp and very little else**, which is worth saying plainly rather than leaving a reader to hunt for a page that changed: `curl -sI https://busmaps.uk/ | grep -i x-app-version` must move off `0.10.0-pilot+e72a1de`, and the development changelog behind the admin `/changelog` route gains the entry. The public *What's new* page cannot see it and should not — `/changelog.html` renders `/data/whats-new.json`, a curated file this change does not touch — measured before this entry was written, which is what makes it evidence: at 11:30Z on 14 September `curl -s https://busmaps.uk/changelog.html | grep -c "landmark control that had never run"` returned **0**, and it must still return **0** afterwards. **This entry also carries a correction, because the list above under-describes what is already live.** The previous entry names `157ec75` and was written at 12:29 on 13 September; the deploy did not run until the morning of 14 September, and three more changes merged in between, so the site that went live as **`e72a1de`** carried `ca87e7f` (the licensing record knew the licensor had answered, #295), **`b461f93`** (the OA-338 de-duplication engine re-vendored, #296) and `e72a1de` (codebase review 2026-09-14 Tier 1, the portal half, #297) with no entry describing any of them — and one of those is an engine re-vendor, which is the ink-moving kind. **That is the gap §3b's ordering does not close**: an entry written in advance describes the change it was written for, and every commit that lands between the writing and the deploy rides up undescribed, which is the same shape as a letter that ages against its own evidence while it waits. Writing the entry earlier would not have helped; deploying the merge that carries it is what §3b actually asks for, and that is the half that did not happen. → **`7a51143`** (2026-09-15, the R1 warning that a re-imported map is a draft and a draft is a 404, #304) — **written before the deploy per §3b, so the sha that gets deployed is the merge carrying this entry and not `7a51143` itself.** **This one ships nothing the public can reach and nothing an operator runs**, read as a diff rather than remembered: two files, `docs/R1-create-map.md` (+11/−3) and one `CHANGELOG.d/` fragment, with nothing under `src/`, `public/`, `engine/`, `schema.sql` or any migration. So `SCHEMA_VERSION` stays at **5**, and no route is added or removed: `npm run check:live-routes` read **102 routes — 64 guarded, 19 public, 10 page, 9 reaches** — every one answering and every refusal a guard rather than a missing route, taken against the live site at 22:19Z on 15 September, and it must read 102 again afterwards. A different count is a reason to stop. **The live read-back that can see this deploy is the build stamp and very little else**, which is worth saying plainly rather than leaving a reader to hunt for a page that changed: `curl -sI https://busmaps.uk/ | grep -i x-app-version` must move off `0.10.0-pilot+344ef63`, and the development changelog behind the admin `/changelog` route gains the entry. The public *What's new* page cannot see it and should not — `/changelog.html` renders `/data/whats-new.json`, a curated file this change does not touch — measured before this entry was written, which is what makes it evidence: at 22:19Z on 15 September `curl -s https://busmaps.uk/changelog.html | grep -c "draft is a 404"` returned **0**, and it must still return **0** afterwards. **This deploy is being run because the drift check demanded it and not because #304 needs to reach anybody**, which is the honest reason and belongs in the record: the twelve-hour grace on `status.js`'s deployment row ran out around 18:40 and turned `buses-data`'s `main` red at 18:48, so a documentation-only commit is the only thing standing between this estate and a green board. **This entry also carries a correction, and it is the SECOND CONSECUTIVE one of the same shape.** The previous entry was written for `aa498a7` and merged as `64536ea` (#300), and said in terms that the deployed sha would be the merge carrying it; the deploy did not run until later and landed on **`344ef63`**, three commits further on, so `0e08bf0` (the owner route gets a door — an owner picker on the admin map view, #301), `7647b87` (deleting a map with a local adviser failed on a foreign key, #302) and `344ef63` itself (pinning the pre-fix arm to a commit, #303) all went live with no entry describing any of them. **One of those three is the ink-moving kind**: #301 is 305 lines of `public/app/editor.js`, 40 of `views/app/editor.html`, a rule change in `src/routes/admin.js` and 15 lines of `public/app/app.css` — a door a human can see and open — and it reached the live site undescribed exactly as the engine re-vendor named in the previous correction did. **Twice in a row is a measurement rather than a slip.** §3b fixes the ordering of an entry against its own change and says nothing about the commits that merge between the writing and the deploy, so the gap is not an accident of timing but the standing behaviour of the rule as written: every entry describes the change its author had in mind, and everything that lands afterwards rides up silent. The remedy is one of two things and the choice is not this entry's to make — either deploy the merge immediately, which is what §3b actually asks for and is the half that has now failed twice, or write the entry at deploy time from `git log <live-sha>..origin/main` so that its subject is the deploy rather than the change. Until one of those is chosen, assume the list above under-describes what is live and read the host, which is the sentence below arriving for the third time. → **`5fc6038`** (2026-09-16, `engine/render.js` re-vendored from the `oa344/file-load-guarantee` branch ahead of the engine merge that changed it, #306) — **written before the deploy per §3b, so the sha that gets deployed is the merge carrying this entry and not `5fc6038` itself** — and this time `git log 7c7c710..origin/main` held exactly one commit when the entry was written, so the list is not under-describing what ships. **What ships is five lines of `require.main` guard in one vendored engine file and its restamped manifest**, read as a diff: `engine/render.js` (+5), `engine/vendored.json` (one hash) and one `CHANGELOG.d/` fragment, with nothing under `src/`, `public/`, `schema.sql` or any migration, so `SCHEMA_VERSION` stays at **5** and no route is added or removed: `npm run check:live-routes` read **102 routes — 64 guarded, 19 public, 10 page, 9 reaches** against the live site at 02:23Z on 16 September, and it must read 102 again afterwards. The guard moves no rendered byte — `render.js` prints the same usage line and exits 1 with no arguments and writes the same JPG with two — so no stored sheet changes, and §4a's `track-engine.mjs` on the host has nothing to bring forward for this file, `render.js` not being a generator. **The read-back that can see this deploy is the build stamp and nothing else**: `curl -sI https://busmaps.uk/ | grep -i x-app-version` read `0.10.0-pilot+7c7c710` at 02:23Z and must move off it. **Why it is deployed at all**: `status.js`'s deployment row read `behind (grace)` the minute #306 merged, and the twelve-hour grace would redden `buses-data`'s `main` in the afternoon of 16 September — the same honest reason as the previous entry, a bookkeeping deploy standing between the estate and a green board. → **`5e05e5a`** (2026-09-16, every named place in Great Britain resolves in the `/maps` search — buses-data OA-312, round 1 of the place-lookup plan, #308) — **written before the deploy per §3b, so the sha that gets deployed is the merge carrying this entry and not `5e05e5a` itself** — and `git log 24d8d76..origin/main` held exactly one commit when the entry was written (the live edge read `X-App-Version: 0.10.0-pilot+24d8d76` at 03:55Z on 16 September), so the list is not under-describing what ships. **What ships is a second stage behind the *Not ours* panel**, read as a diff: `src/search/places.js` (new, 202 lines), `src/search/directory.js` (+117/−68, the place stage and the shared `normalize()`), `public/js/shared/map-card.mjs` (the sentences), `src/routes/public.js`, `public/js/public-maps.js`, `public/css/styles.css`, `public/legal.html` (the attribution), `scripts/sync-directory.mjs` and two test files, `CLAUDE.md`, one `CHANGELOG.d/` fragment, and **three data files vendored server-side under `src/search/data/`** — `places.json` (1.2 MB, 59,415 places), `lad-to-lta.json` and `places-source.json` — with nothing under `engine/`, `schema.sql` or any migration, so **no engine change, no re-vendor, no `track-engine.mjs`, no `Caddyfile` change, `SCHEMA_VERSION` stays at 5** and no route is added or removed: `npm run check:live-routes` read **102 routes — 64 guarded, 19 public, 10 page, 9 reaches** at 03:55Z and must read 102 again afterwards. Nothing to configure: the three files ship inside the image and are read once at first use. **The §3a method is the only thing that can see this deploy, and four readings were taken at 03:56Z so that afterwards means something.** `https://busmaps.uk/maps?q=Harrogate` carried `nothing in it matches` once and `Harrogate</strong> is in` **zero** times — afterwards the first must be gone and the second present, the panel saying *Harrogate is in North Yorkshire, whose local transport authority is York and North Yorkshire Combined Authority*, with the letter beneath the card. `?q=Kirkwall` carried `is in Scotland` **zero** times and must carry it once. `?q=Cambridge` carried `is part of this authority` once and **must still**, because that is the control that the directory's own matching keeps winning. `https://busmaps.uk/legal.html` carried `Index of Place Names` **zero** times and must carry it. And `https://busmaps.uk/data/places.json` answered **404** and must go on answering 404, because the place data is server-side only and a 200 there would mean somebody had put 1.2 MB under `public/`. The build stamp must move off `0.10.0-pilot+24d8d76`. → **`3aaa06f` and `0c84734`** (2026-09-16, the place index reaches maps published by script, and five things a reader found in the `/maps` search — buses-data OA-379 and OA-380, #310 and #311) — **written before the deploy per §3b, so the sha that gets deployed is the merge carrying THIS entry and not either of those** — and `git log 08d1b19..origin/main` held exactly those two commits when the entry was written, with the live edge reading `X-App-Version: 0.10.0-pilot+08d1b19` at 05:35Z, so the list is not under-describing what ships. **What ships, read as a diff rather than remembered**: two `CHANGELOG.d/` fragments, `package.json` (three npm script NAMES and not one dependency), four files under `public/` (`js/shared/map-card.mjs`, `js/public-maps.js`, `js/contact-kind.js`, `contact.html`), nine under `scripts/` and two under `src/` (`routes/public.js`, `http/helpers.js`) — with **nothing under `engine/`, `src/db/`, `schema.sql`, any migration, the `Dockerfile`, `compose.yaml` or the `Caddyfile`**, so no engine change, no re-vendor, no `track-engine.mjs`, no `Caddyfile` change, `SCHEMA_VERSION` stays at **5** and nothing is configured at deploy time. No route is added or removed either: `npm run check:live-routes` read **102 routes — 64 guarded, 19 public, 10 page, 9 reaches** against the live site at 05:30Z, and it must read 102 again afterwards; a different count is a reason to stop. **THE UNUSUAL HALF OF THIS RELEASE HAPPENED BEFORE IT, AND A LATER READER NEEDS IT TO MAKE SENSE OF THE READINGS BELOW.** OA-379 is the fault that `scripts/publish-baseline.mjs` moved the published pointer without writing a version's `places.json` sidecar, so the first real customer's four St Neots maps answered the `/maps` search by their own titles and by nothing else. The code fix stops it recurring; it does **not** repair the four maps already in that state, and `docker compose exec -T portal node scripts/build-place-index.mjs` was run on the host beforehand to do that. Those sidecars are therefore already on disk and **invisible**, because `src/search/index.js` builds its index once per process against a counter no other process can bump — **so the container recreation in step 4 of the upgrade is what makes them live, rather than any line of code in this deploy.** Somebody reading afterwards and wondering why four maps became searchable by their villages without a republish should read that sentence rather than look for a migration. **The §3a method is the only thing that can see any of this, and seven readings were taken at 05:34Z so that *afterwards* means something — every one of them zero, which is what makes them evidence.** `https://busmaps.uk/maps?q=Tilbrook` and `?q=Boxworth` each carried `href="/m/st-neots"` **zero** times and must both carry it afterwards — Tilbrook because route 150 terminates there and Boxworth because the 69 does, both printed on the published sheet all along. On all three of `?q=Tilbrook`, `?q=Boxworth` and `?q=Eynesbury` the element read `id="searchMeta" hidden>` with nothing inside it; afterwards `?q=Eynesbury` must carry **No exact match for “Eynesbury” — showing results for “Aylesbury”.**, which is the fault Peter reported — a shared link explained nothing while typing the same query into the box explained it. `covers <strong>Tilbrook</strong> yet` read **zero** and must read one. `target="_blank"` read **zero** on the whole page and must be present, on the links that leave this site for a council's. `Open their map page` read **zero** and must be present. `kind=map-request` read **zero** on `/maps?q=Tilbrook` and must be present, and `https://busmaps.uk/contact.html` carried `value="map-request"` **zero** times and must carry it once — those two are one door and one form, and either alone is a broken link. **And one reading that must NOT move, stated here so a later reader does not take it for a regression**: `?q=Eynesbury` must go on returning no St Neots map. Nothing in this release indexes a map's INTERNAL sheet, and Eynesbury is named only there — as the terminus of the 69 and of the 61EY town circular, in `internalDesc` — so no sidecar however correctly written carries it. That is a known, deliberate remainder and not something this deploy failed to do. The build stamp must move off `0.10.0-pilot+08d1b19`. **THE READ-BACK CONTRADICTED THAT LAST SENTENCE, SO HERE IS THE SECOND ENTRY §3b ASKS FOR** (2026-09-16, deployed as **`f0d511f`**). Everything else in the entry above held: the stamp moved to `0.10.0-pilot+f0d511f`, `check:live-routes` read **102 — 64 guarded, 19 public, 10 page, 9 reaches**, and all seven zero readings went non-zero — `?q=Tilbrook` returns the St Neots area sheet explaining itself as *Route 150 goes to Tilbrook*, `?q=Boxworth` three maps by way of the 69, `?q=Neotts` carries *No exact match for “Neotts” — showing results for “Neots”.* in the served HTML where a shared link explained nothing before, `?q=Harrogate` and `?q=Colchester` say *covers … yet* and offer both doors, and the directory rows carry `target="_blank"`, `Open their map page ↗` and `kind=map-request`. **What was wrong is the remainder.** `?q=Eynesbury` returns **two maps** — St Neots Co-op (*Route 61EY goes to Eynesbury Tesco*) and St Neots Town Centre (*Route 150/61EY goes to Tesco / Eynesbury*) — because `buildPlacesFromDir()` has **two branches**, and a PLACE map's is `destinations[]` rather than an area map's `external[]`. Those two packs name Eynesbury there; the third, St Neots Tesco Extra, does not, and the St Neots AREA sheet still does not, so `href="/m/st-neots"` is genuinely absent from that query and the narrow claim was true. **The entry generalised it to the page, having read one branch of a two-branch function and not the other** — which is why the sentence is corrected here rather than quietly edited: a later reader using *Eynesbury returns nothing* as a control would be misled by it, and the shape is this estate's own *the control that held on one town*. What remains true is the smaller statement: **nothing indexes a map's INTERNAL sheet**, so a name that appears only in `internalDesc` — as Eynesbury does on the area sheet, terminus of the 69 and of the 61EY — reaches the search only if some other pack happens to carry it as a destination. → **`fd0f058`** (2026-09-18, `0.10.0-pilot`, buses-data OA-367 — `POST /api/admin/applications/:id/approve` asks about the ORGANISATION before the person, so a second application row for an organisation already registered is refused as a duplicate that names the existing customer and says to reject it, instead of telling the operator to *"approve this organisation manually"*, which names a screen that has never existed; the genuine collision — a new organisation whose contact already holds an account — is refused honestly and names no script, because `user.email` is `UNIQUE` and a user row carries one `customer_id`, so no script does that job; and the three writes underneath both refusals, `insertCustomer` → `insertUser` → `setApplicationReviewed`, are now one transaction through a new `withTransaction()` in `src/db/index.js`, so a throw between the first and the third no longer leaves an orphan customer with no users and the application still pending). **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **There is no visible change to any public page** — this is an admin route, a refusal message and a transaction — so `check:live-routes` and the `/health?deep=1` read-back are the whole of what §3a can see here, plus `X-App-Version` moving off `0.10.0-pilot+feba460`. **The §3a check that can actually see this fix is none of them**, and saying so is the point: the only way to exercise it is to approve an application, which writes a customer and mails a stranger, so it is deliberately NOT part of the smoke test; what stands under it instead is `scripts/test-approve-application.mjs`, which forces `insertUser` to throw through the route with a `BEFORE INSERT` trigger and requires the rollback. **The baseline this entry starts from is not the entry above it.** The list ended at **`f0d511f`** while the running site reported `0.10.0-pilot+feba460`, so the four merges between them — `d74fb97` (#314), `ab2cb92` (#315), `9bbeaf7` (#316) and `feba460` (#317) — reached busmaps.uk with no entry here describing them. This entry does not reconstruct them; it records the gap where a later reader will otherwise read continuity, and that gap is buses-data OA-377 happening for the third and fourth time. → **`cb5cd35`** (2026-09-19, `0.10.0-pilot`, the three engine files OA-340's opt-in `pubs` category moved — `poi_select.js`, `services_panel.js`, `icons.js` — re-vendored from `claude-skills` PR #29 while that branch was still open, which is the order that completes: this repository's `check-vendored.mjs --no-skills` verifies hashes only and does not ask whether the skill has moved on, while `claude-skills`' `status` job reads THIS repository's `main`, so the engine pull request cannot go green until this one has merged). **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing on any public page moves and no stored byte changes**: no map anywhere carries `poi.include: ["pubs"]`, so the new `classify()` clause is unreachable on every sheet that exists — measured rather than argued, by recutting the engine's own five-map fixture estate on the branch, where the entire diff was five stamp lines and no ink. **The half §3a cannot see is the live map store**, and it is the one that matters here: a delivered map's pack carries its own copy of the generators (OA-130, track not freeze), so deploying a re-vendored `engine/` reaches the portal and does not reach the maps already in it. `scripts/track-engine.mjs` on the LAPTOP brought five local packs forward from `d22b1e563aa0` to `f95164e74abc` — that number is about the dev store and is not an answer about busmaps.uk — so §4a's live-store form is run after this deploy and its `0 BEHIND, 0 skipped` is the finishing state. → **`f889abe`** (2026-09-19, `0.10.0-pilot`, H1 item 5 of the pre-customer checklist — the ten S6 correctness deferrals in `scripts/s6-waivers.json` were dead letters, so the `waive` array is empty and the assertion that could only pass while they existed is rewritten. Measured over all 20 maps through `scripts/lib/s6-freshness.mjs`: 20 of 20 read `fresh` and every one carries `verdict: "pass"` at 0 hard findings, and `gateS6()` in `scripts/deliver-map.mjs` returns at `fresh` **before** it consults the waiver file — so while the estate is in that state not one of those entries could affect a delivery, and Huntingdon's `until` expiring on 15 September was a non-event. The rows were cleared the way the file's own header demands, by running S6 rather than by moving a date, the newest runs dated 12–13 September.) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing on any public page moves, no route changes and no stored byte changes**: the three files are a JSON deferral list this repository reads only inside `deliver-map.mjs`, one row of `docs/H1-operations-handbook.md`, and a test. **So the only §3a reading that can see this deploy is `X-App-Version` moving off `0.10.0-pilot+96e20e8`** — `check:live-routes` and `/health?deep=1` will read exactly as they did before it, written down here so a later reader does not take an unchanged count for a release that never landed. **And the check that actually stands under the change cannot run in CI, which the entry says rather than leaves to be discovered**: `verification.json` is gitignored in `buses-data`, so from a clone all 20 maps read `no-verdict` and all 20 refuse. A green `test` run here proves the array's SHAPE and the five prove-red arms behind it, never the estate's freshness, which rests on the operator's own S6 runs on the laptop. The file is **kept rather than deleted**: an empty array is the state it exists to reach, and the fourth prove-red arm requires a non-empty array to stay legal. → **The second entry for `503e571`, because the read-back surprised us** — not about the release, which was clean, but about the instrument. **The deploy itself is unremarkable and is recorded here so the fault below is not mistaken for one**: merged 2026-09-19 at 05:55Z, deployed at `builtAt 2026-09-19T06:14:53Z`, `/health?deep=1` **200** with `gitSha: 503e571` and all five checks green (`sharp 0.35.4` / `vips 8.18.6`, email `provider: resend`), `check:live-routes` **102 routes unchanged**, and `X-App-Version`, the host's `git rev-parse HEAD` and the container's own `GIT_SHA` all three reading `503e571`. **What surprised us is that `npm run deploy` could not read `gitSha` at all, and said the wrong thing about why.** Its step 5 got `METRICS_TOKEN` off the host with `{ set -a; . ./.env 2>/dev/null; set +a; }`, and that aborts: line 3 of the host's `.env` is `EMAIL_FROM=Peter Cooper - BusMaps.uk <info@busmaps.uk>`, which Compose reads happily and a shell cannot parse at all, because `<` is a redirection operator with a newline where a filename should be. The shell gave up there; `METRICS_TOKEN` is on line 11 and was never set; the `2>/dev/null` swallowed the syntax error; and the `else` arm then announced *"the HOST .env has no METRICS_TOKEN"* about a value that is present and 48 characters long in both the file and the running container. **So every deploy since `EMAIL_FROM` took that form has ended without the one field the step exists to read, under an explanation that was not true** — the shape this project already names *the warning about the instrument*: a check that could not RUN, wrapped in a sentence asserting a finding about its subject. **The readings above were taken by hand**, with `sed` in place of the sourcing, which is also the fix: `scripts/lib/host-env.mjs` now reads `.env` with Compose's grammar rather than the shell's, and the `else` arm distinguishes *there is no such line* from *the line is there and read back empty* instead of asserting the stronger one. `npm run test:deploy-env-probe` runs both fragments under a real `sh` against a fixture carrying that same `EMAIL_FROM` line, and its first arm requires the OLD form to come back empty on it — so the suite reproduces the fault rather than only avoiding it. Falsified by restoring the sourcing probe: five arms redden, the file restored byte-identically afterwards. → **`7c75408`** (2026-09-19, `0.10.0-pilot`, the deploy stops sourcing the host's `.env` — `scripts/lib/host-env.mjs` reads `METRICS_TOKEN` with Compose's grammar instead of the shell's, and step 5's `else` arm distinguishes *there is no such line* from *the line is there and read back empty* rather than asserting the stronger one. The entry immediately above has the fault and the measurement.) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing on any public page moves, no route changes and no stored byte changes** — `check:live-routes` should read 102 unchanged, and the only §3a reading that can see the release is `X-App-Version` moving off `0.10.0-pilot+503e571`. **The thing worth writing down before the deploy, because it is easy to misread afterwards: this fix takes effect from the LAPTOP, not from the host.** `deploy.mjs` runs out of this working tree; the container never executes it. So the new probe is live the moment the merge is checked out here, and shipping the image is not what makes step 5 work — a later reader comparing the host's copy of `scripts/` against the behaviour they saw would otherwise conclude the two disagree. **And the deploy that ships it is also its acceptance test**, which is the one reading that is genuinely new: step 5 must print a `/health?deep=1` body CARRYING `gitSha`, `builtAt` and `checks{}` and equal to the merge commit, where every deploy before this one printed the four-field anonymous answer under the untrue sentence *"the HOST .env has no METRICS_TOKEN"*. If it still prints the short form, the `else` arm now says which of the two empties it found, and that is the thing to follow rather than a guess. → **`b91c01b`** (2026-09-19, `0.10.0-pilot`, the admin console stops claiming an invite was emailed when it does not know — the approve route and `POST /admin/users` return `emailed` and `emailError` the way the adviser route already did, the banner shows three outcomes and hands back the sign-in link whenever nothing was sent, and the Ops tab renders the `activity.email` delivery counters that have been in `/api/admin/ops` since P7 and on no screen. buses-data OA-362.) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing public moves**: every edit is behind the admin door, no route is added or removed — `check:live-routes` should read 102 unchanged — and no stored byte changes. **The §3a reading that can see it is `X-App-Version`**, moving off `0.10.0-pilot+6d33053`; the admin console itself is the only place the change is visible, and it is not anonymously reachable. **The one thing worth writing down in advance, because the deploy cannot check it:** the Ops email card reports counters held **in memory**, so the deploy that ships it also zeroes them, and the card will read *last sent: never, since this process started* immediately afterwards however healthy the provider is. That is the card working, not a regression, and it says so on its own face. → **`1c1e851`** (2026-09-19, `0.10.0-pilot`, `trustProxy` stops being a number — a numeric `trustProxy` trusts *nothing* in Fastify, which its own shipped test says in its title, so `req.ip` has been the Docker bridge gateway for every visitor and the four public POSTs were rate-limited in total rather than per person. The value becomes `'loopback, uniquelocal'` in `src/http/trustProxy.js`. buses-data OA-256.) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing public moves**: no route is added or removed — `check:live-routes` should read 102 unchanged — no page changes and no stored byte changes. **The §3a reading that can see it is `X-App-Version`**, moving off `0.10.0-pilot+fbfacd0`; the change is a property of how requests are *attributed*, which no anonymous response body reports. **The two things worth writing down in advance, because the deploy cannot check either:** the rate-limit counters live in a per-process `Map`, so this deploy empties them exactly as every deploy does, and anyone mid-limit is forgiven — the failure mode is a moment's leniency, never a lockout; and the evidence OA-256 actually asks for is in the **app's own log on the host**, where `remoteAddress` should now read a real visitor's masked /24 rather than `172.18.0.0` on every line. That read needs a shell on the box and is not part of this deploy. → **`1ee8471`** (2026-09-19, `0.10.0-pilot`, the customer invite email stops being three lines that name nobody — the letter the first person this project ever invited received named neither him, nor his organisation, nor the sender, nor what he would be signing in to, while `adviserContent()` twenty lines below it in the same file was a careful seven-paragraph letter. `inviteContent()` in `src/email/index.js` is now that letter for a customer's editor, `orgName` travels from both call sites in `src/routes/admin.js`, and the add-user route also passes `role` because two of the paragraphs are true only of an editor — an approver publishes. The stale-link sentence is shared rather than copied. buses-data OA-365.) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing public moves**: no route is added or removed — `check:live-routes` should read 102 unchanged — no page changes and no stored byte changes. **The §3a reading that can see it is `X-App-Version`**, moving off `0.10.0-pilot+1e45aea`; the change is the text of an email, which no anonymous response body reports. **The one thing worth writing down in advance, because the deploy cannot check it:** nothing on the box sends this letter until an admin approves an application or adds a user, so **the deploy cannot demonstrate the fix at all** — the first real evidence is the next invite that goes out, and `npm run test:magic-link-email` is what stands in for it until then. That asymmetry is the whole reason this action exists: the fault was found by a person reading a real email, and no gate in this repository could see it. → **`c97a2b9`** (2026-09-20, `0.10.0-pilot`, the four render routes an editor can reach get a per-user budget — `technical-audit_2026-08-19` O7's second half, which was recorded done on buses-data OA-039 while the routes that actually spawn a generator had no bound at all. `renderBudgetSpent()` in `src/http/helpers.js` keys `rateLimited()` on `render:<user id>`, a namespace inside the one counter map rather than a second one, and the preview and save routes in `src/routes/editor.js` and the proposed-update pair in `src/routes/proposed.js` all spend it. The three expert diagram routes are admin-only and deliberately out of scope. buses-data OA-039, [#325](https://github.com/PeterC66/community-bus-maps/pull/325).) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing public moves**: no route is added or removed — `check:live-routes` should read 102 unchanged — no page changes and no stored byte changes. **The §3a reading that can see it is `X-App-Version`**, moving off `0.10.0-pilot+d73b910`. **The one thing worth writing down in advance, because the deploy cannot check it:** the budget is observable only by exceeding twenty renders in a minute as a signed-in editor, which is a thing to do to a test instance and not to the live one — so **the deploy cannot demonstrate this fix either**, and what stands in for it is `npm run test:render-budget` and `npm run test:prove-red-render-budget`. Two consecutive entries now say the deploy cannot see the change they carry; that is worth noticing rather than repeating, and what it is measuring is that the cheap post-deploy readings answer *is the site up and unchanged*, which is a different question from *did the thing I shipped work*. → **`0a8af12`** (2026-09-20, `0.10.0-pilot`, both customer-row tooltips stop telling the operator to turn `Watermark downloads` off alongside `Sample maps` — the first real customer asked to keep the watermark ON after registering, which is the case that separates two flags every earlier customer moved together, so each tooltip now says the other is a separate decision and the watermark’s says it is the organisation’s rather than ours. Carrying with it one clause of §3b, which called all four post-deploy readings reproducible at any time when `builtAt` is served only in the `opsAuthorised()` branch of `/health`. buses-data OA-357 and OA-334 step 4, [#326](https://github.com/PeterC66/community-bus-maps/pull/326).) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing public moves and nothing server-side moves at all**: the whole change is two `title` attributes on an admin-only screen and two paragraphs of documentation, so `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after, no stored bytes change, no route is added or removed, and `SCHEMA_VERSION` is untouched. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+42d6c6d`. **What the deploy cannot check, and it is the whole point of the change:** a `title` attribute is invisible to every fetch — it is read by a person hovering a checkbox on `/app/admin`, behind a sign-in this laptop does not hold open — so the post-deploy evidence for this entry is that the new strings are in the served `admin.js`, and nothing more. That is the third consecutive entry whose change the deploy cannot demonstrate, which is now a pattern rather than a coincidence: the cheap read-backs answer *is the site up and unchanged*, and a fix whose entire surface is what a human reads has no reading of that shape at all. → **`ba72447`** (2026-09-20, the delivery gate that asks whether `--src` is the render the map's own `manifest.json` calls current — step 0b in `deliver-map.mjs`, beside the S6 gate and before the `scp`, with `--render-superseded "<reason>"` as the one-off override. It is the question the pre-flight byte gate was never asking: on 2026-09-15 three of the first customer's four maps were delivered from renders 10, 10 and 23 builds old, the byte gate refused one and passed the other two, and two of those sheets are publicly live a fortnight stale. buses-data OA-368 item 3, [#328](https://github.com/PeterC66/community-bus-maps/pull/328).) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing served moves and nothing server-side moves at all**: every changed file is a laptop-side script, its tests, or documentation — `scripts/deliver-map.mjs` and `scripts/lib/newest-render.mjs` are run by an operator and by nothing inside the container, and `grep` for either in `engine/vendored.json` returns 0, so no map goes stale and no engine pin moves. `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after; no stored bytes change, no route is added or removed, and `SCHEMA_VERSION` is untouched. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+7acaeb6`. **What the deploy cannot check:** nothing in this change is reachable over HTTP at all, so the post-deploy evidence is that the site is up and unchanged and the sha has moved — the fourth consecutive entry of that shape, and the pattern the entry before this one named. The gate's own evidence is its suite and its seven prove-red arms, which run in CI on every push and are the only thing that can demonstrate it. → **`35000ff`** (2026-09-20, `0.10.0-pilot`, **re-vendor `engine_paths.js`**: the engine’s module resolver no longer falls back to a hard-coded path to the engine installed on one laptop — it refuses. That arm is how eight hybrid sheets reached `main` in September; buses-data OA-342 item 5, upstream [claude-skills#64](https://github.com/PeterC66/claude-skills/pull/64), portal [#329](https://github.com/PeterC66/community-bus-maps/pull/329).) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **Nothing served moves, and here that is measured rather than reasoned**: the refused arm is unreachable from this repository because `renderMap.js` always passes `SKILL_ASSETS` at `engine/`, and `scripts/test-engine-selfsufficient.mjs` demonstrates it by RUNNING all three sheet generators and listing every module each one loaded — 34 loads across `internal`, `external` and `internal-schematic`, every one of them from `engine/` or the map’s own data pack and none from anywhere else. `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after; no stored bytes change, no route moves, and `SCHEMA_VERSION` is untouched. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+6d6086b`. **What the deploy cannot check:** this file is read only while a sheet is being rendered and a deploy re-renders no stored sheet, so the post-deploy evidence is that the site is up and unchanged and the sha has moved — **the fifth consecutive entry of that shape**. **What it DOES owe, and it is not new:** `engine/` has moved, so the LIVE map store is one file further behind until `npm run track:engine` is run against the host (§4a) — the condition already raised in the loop’s `live-map-store-not-tracked-after-the-pubs-revendor` hold, which this adds a second file to rather than creating. → **`c9707a1`** (2026-09-20, `0.10.0-pilot`, **the configured sender becomes readable**: `checks.email` on `/health?deep=1` now carries `from` verbatim in every branch including the fault branches, and `emailHealth()` spreads the same object so the ops snapshot and the worklist get it too; buses-data OA-350, portal [#299](https://github.com/PeterC66/community-bus-maps/pull/299).) **The sha above is the CHANGE; the deployed sha is the merge commit carrying this entry**, per §3b. **What it is for.** `EMAIL_FROM` decides what a stranger sees in their inbox and it lives only on the host: no repository held it and no probe returned it, so the only way to learn it was to send yourself an email and look. On 14 September a session inferred it instead from the `8493db1` entry in this very history — which *recommends* a named sender — found nothing recording that the recommendation had been acted on, concluded nobody had, and rewrote a true sentence in a letter to a member of the public into a weaker one naming only the address. Peter had set it two days earlier. **So the other half of OA-350 is still open and it is in this file**: the `8493db1` entry still reads as a description of the host rather than as the recommendation it was, and the first person to read `checks.email.from` against it is the person who can finally correct it. **Reported and deliberately not graded**: a bare address is a poorer sender, not a broken one, and `ok:false` here drives a 503 on `/health?deep=1` and pages an operator, so a control asserts that a bare `EMAIL_FROM` stays healthy and is still reported as it stands. `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after; no stored bytes change, no route is added or removed, and `SCHEMA_VERSION` is untouched. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+9ae9772`. **What the deploy cannot check from this laptop, and it is the new field itself**: `checks{}` has been gated behind `METRICS_TOKEN` or a signed-in admin since 2026-08-20 and this laptop holds neither, so the anonymous `/health?deep=1` answers `status`, `service`, `version` and `time` and nothing else. The post-deploy evidence is therefore that the site is up, the sha has moved on `X-App-Version` — §3b's own substitute for the `gitSha` line — and `check:live-routes` still reads 102; the value of `from` has to be read by whoever next opens that endpoint with a credential, and reading it is the act that closes the rest of OA-350. → **this PR's merge** (2026-09-21, `0.10.0-pilot`, the OA-250 item 2 re-vendor: `engine/poi_select.js` and `engine/place/gen_internal.js` move together so the vendored engine matches [claude-skills #68](https://github.com/PeterC66/claude-skills/pull/68), which makes a build say when a landmark somebody marked `must` was culled by the sheet's frame or its blank coreBox — High Wycombe drops 31 classified places, three of them `must`). **Written in advance, per §3b**, and the sha this entry names is the change while the sha deployed is the merge carrying it. **This deploy is expected to change nothing a reader sees, and that is the claim to check.** The engine change draws no new ink: all nine committed `internal.svg` sheets in `buses-data` reproduce byte for byte against it, and `npm run verify:place` here reproduces four shipped place sheets byte for byte from the re-vendored copies — so no stored bytes change, no route is added or removed, `SCHEMA_VERSION` is untouched, and `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+a8e4661`. What the deploy cannot check from this laptop is unchanged and is not new here: `checks{}` needs `METRICS_TOKEN` or an admin session and this machine holds neither, so the post-deploy evidence is that the site is up, that the sha has moved on `X-App-Version`, and that the route count still reads 102. → **this PR's merge** (2026-09-21, `0.10.0-pilot`, the signed-in wording follow-up to OA-404: customer- and adviser-facing strings in the editor, the landmark chooser, the adviser page, the build-warning headings and three server errors say *map* / *map set* where they said *sheet*, which also ships #334's docs-only merge `4453ccc`). **Written in advance, per §3b**; the change is this PR's commit and the sha deployed is its merge. It moves no ink and no stored byte — every edit is a UI or error string, no route is added or removed and `SCHEMA_VERSION` is untouched, so `check:live-routes` must read the same count after as before. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+6c2cc54`; a signed-in page showing *See the real map* on the landmark chooser is the visible check. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **this PR's merge** (2026-09-21, `0.10.0-pilot`, three commits bundled into one deploy because none of them was deployed on its own: `ad4d7c8` #336 makes `audit.yml`, `render-parity.yml` and both `test.yml` jobs read their Node major off the Dockerfile `FROM` line the way `verify.yml` already did, and has `test-node-pin.mjs` fail on any literal or `lts/*` pin instead of missing it; `049e536` #337 has the portal declare its own `.doc-acronyms.json` exclusions and short-forms table and run `check:doc-acronyms` in CI (201 documents/66 short forms before, 32/12 after, exit 0); `a755832` #285 adds `trap 'exit 1' PIPE` to this repo's own `.githooks/pre-commit` so a long refusal piped through `head` can no longer take SIGPIPE and exit 0 with the bad commit landed anyway (measured on this repo's hook: 16 leaks in 24 runs before, 0 in 24 after). **#336 and #337 merged before this entry was written** — the gap a scheduled tick found and is closing here, entry-before-deploy rather than entry-before-merge, because by the time it looked both were already on `main` and undeployed. None of the three touches a served route, a rendered byte or `SCHEMA_VERSION`: `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+21ce4f7`. → **this PR's merge** (2026-09-21, `0.10.0-pilot`, PR #284: R2 now has a Step 2a telling the operator to turn *Sample maps* off before a real customer publishes, corrects two claims that were wrong when read against the code — there is no admin-console reassign-owner button, and the reconciliation route can report `{error: true}` with nothing rendering it — and fixes the restamp path in both `PILOT.md` and `R2-onboarding.md` from the nonexistent `/srv/busmaps` to `/opt/community-bus-maps` (`DEPLOY_APP_DIR`). **Docs only**: no route, rendered byte or `SCHEMA_VERSION` moves, so `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+c20a0c1`. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **f2ccc1c** (2026-09-21, `0.10.0-pilot`, PR #344: the homepage's pilot banner, hero lead and published-maps strip intro stop asserting the site has no customers, now that Love's Farm Community Association's own maps sit two screens below the banner with no Sample badge — `src/config.js`'s `PILOT.long` and two strings in `public/index.html`, plus `published-strip.js` deriving its "marked Sample" sentence from what it actually rendered instead of asserting it as a standing rule; buses-data OA-433). **Entry-before-deploy rather than entry-before-merge**, the same gap as the entry above: by the time this was written, #344 was already on `main` and undeployed. No route, rendered map byte or `SCHEMA_VERSION` moves: `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+804e64d`. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **dc72cbc** (2026-09-22, `0.10.0-pilot`, PR #346: `warnIfStaleSibling()` stops printing a false stale-sibling warning on every area delivery — it treated `path.dirname(FIXTURE_DIR)` as the folder of sibling renders even when that parent is a container's filesystem root (`/fixture`, mounted read-only by `deliver-map.mjs`'s pre-flight verify), where every real directory reads as newer than the fixture; the guard now skips the comparison unless the fixture is a real `<town>/S5-render/<run>` folder, reusing `newest-render.mjs`'s `versionedRender()` (now exported) rather than a second check for the same shape; new `test-fixture-freshness.mjs`; buses-data OA-432). No route, rendered map byte or `SCHEMA_VERSION` moves: `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+715579d`. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **30303d8** (2026-09-22, `0.10.0-pilot`, PR #348: the landmark chooser stops mis-labelling a place the sheet's frame excluded — `genWarnings.js` gains a fourth `poi.tiers:` arm, FIRST, keyed on `fell off this sheet`, so an editor is told *Some places you chose are off the edge of the map* instead of the two wrong sentences the three older arms produced: a culled line names its `must` count, so it contains the string `"must"` and the placement arm claimed it, and one with no `must` in it fell through to *named nothing on this map and did nothing*, false in each of its three clauses; the fourth test constant is built by calling the engine's own `culledAfterTiersNote()` rather than quoted from it, with the verbatim convention kept as two assertions of its own; buses-data OA-250 item 2). No route, rendered map byte or `SCHEMA_VERSION` moves: `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+bfbeb23`. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **`1490a8b`** (2026-09-22, `0.10.0-pilot`, PR #350: every `prove-red-*` harness stops borrowing `node_modules` from a checkout that may not have one — eighteen of them junctioned `path.join(ROOT, 'node_modules')` into their scratch copy, which in a git worktree is a path that does not exist, so the copy ran with no dependencies and seventeen harnesses reported `control RED` while the code they test was green; new `scripts/lib/scratch-deps.mjs` asks `git rev-parse --git-common-dir` instead of guessing, and THROWS where no `node_modules` can be found rather than letting three of them skip silently; new `scripts/test-scratch-deps.mjs` in `npm test` checks the resolver against two real worktrees it builds, and the rule that says all eighteen go through it; buses-data OA-440). **Nothing in this change runs on the host**: it is `scripts/` and one `package.json` script name, so no route, rendered map byte or `SCHEMA_VERSION` moves — `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+723564b`. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **`343b113`** (2026-09-23, `0.10.0-pilot`, PR #352: the caveats on `terms.html` and `legal.html` stop counting organisations — *"with one organisation signed up and no income"* becomes *"and has no income: no organisation pays for it"*, because a count typed into a legal page goes stale the day a second organisation signs up; the terms page's Working draft caveat and its *last reviewed 25 July 2026* date are unchanged, deliberately, because buses-data OA-138's park rests on both; `docs/PILOT.md` records where the count now lives). **Only two static pages change**: no route, rendered map byte or `SCHEMA_VERSION` moves — `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+91a8ec5`, and the live `terms.html` must carry the new sentence. The change is `343b113`; the commit deployed is the merge carrying this entry. Deployed by a session under R1 ([§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it)). → **`36f642b`** (2026-09-23, `0.10.0-pilot`, PR #355: `engine/place/gen_external_places.js` re-vendored from claude-skills#4 (`f4004c6`) — the place external sheet takes its page size from `page.js` rather than its own copy (buses-data OA-322, no ink), its footer says when it was cross-checked, e.g. *"cross-checked with operators (July 2026)"*, and prints nothing when `checkedAt` is absent (OA-321), and the note's last two words are joined by a no-break space so the footer never ends on one word, which three place sheets did; `gate-fixtures/` carries the High Wycombe Aldi external recut in buses-data `bc490cda`. PR #354, a gate-fixture re-vendor that runs nothing on the host, rides along). **No stored sheet changes on the day**: the generator is used by the NEXT render of a place map, and only after `npm run track:engine` has been run inside the container on the VPS, which is the step that follows this deploy; no route or `SCHEMA_VERSION` moves — `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+d92bc79`. The change is `36f642b`; the commit deployed is the merge carrying this entry. Deployed by Peter. → **`2e084c0`** (2026-09-23, `0.10.0-pilot`, PR #357: `engine/place/gen_external_places.js` re-vendored again from claude-skills#98 (`66f6e82`) — a month-and-year date such as *"(August 2026)"* in a place external's footer is joined by a no-break space so it can no longer split across two lines, which it did on Ely Co-op once #355's generator was rolled out; `gate-fixtures/` carries the one-byte recut of the High Wycombe Aldi external from buses-data `0023ed81`). **Deployed together with `36f642b` above, which has not been deployed on its own**: the live site still reads `0.10.0-pilot+d92bc79`, so this entry's merge is the one commit deployed for both. No stored sheet changes on the day, and no route or `SCHEMA_VERSION` moves — `check:live-routes` read **102 (64 guarded, 19 public, 10 page, 9 reaches)** before it and must read 102 after. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+d92bc79`. Then, inside the container, `track-engine.mjs` and `track-engine.mjs --apply` as given above, so stored place packs pick up the generator. The change is `2e084c0`; the commit deployed is the merge carrying this entry. Deployed by Peter. → **PR #359** (2026-09-24, `0.10.0-pilot`, buses-data OA-390: `docs/LICENSING.md` §3 names the bustimes.org site owner by role, reports his reply rather than quoting it, and points at `Correspondence/CORR-009/` in the private `buses-data` repository; the same name goes from §3's table row and one archived changelog entry). Documentation only: no public page, no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+f69956c` before it, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+f69956c` to the merge carrying this entry. Deployed by the scheduled loop (sched-0019). → **PR #360** (2026-09-24, `0.10.0-pilot`, buses-data OA-425: `rotate-secret.mjs` fingerprints the old secret the way Compose reads the key — last line, one pair of quotes stripped — through `scripts/lib/host-env.mjs`, and its host script moves into `scripts/lib/rotate-host-script.mjs` so `test-rotate-secret.mjs` can run it). Laptop-side script and its test only: no public page, no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+053df71` before it, which is `origin/main`, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+053df71` to the merge carrying this entry. Deployed by the scheduled loop (sched-0109). → **PR #361** (2026-09-24, `0.10.0-pilot`, buses-data OA-447: every `public/*.html` page and the public 404 page open with a *Skip to main content* link to `<main id="main" tabindex="-1">`, `check-chrome.mjs` fails a page without that target, the pilot banner is inserted after the skip link, and the decorative emoji — the `.ico` card icons and the logo — carry `aria-hidden="true"`). Public pages and `styles.css` change: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+8446924` before it, which is `origin/main`, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+8446924` to the merge carrying this entry, and the homepage must serve the skip link. Deployed by the scheduled loop (sched-0157). → **PR #362** (2026-09-24, `0.10.0-pilot`, buses-data OA-295: a public map's `publishedAt` is the time its live version was **approved**, meaning the latest approved `publish_request.reviewed_at`, and falls back to the version's creation time. Before this it was the version row's creation time. It moves the *Published* pill on every public map page, the map cards' *updated* date, the sitemap's `lastmod` and `/api/public/maps`, and `listPublicMaps()` is now ordered by it). One SQL expression in `src/db/index.js` and its test: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+fff46ff` before it, which is `origin/main`, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+fff46ff` to the merge carrying this entry, and Ramsey's `publishedAt` in `/api/public/maps` must no longer read `2026-09-08 15:00:19`. Deployed by the scheduled loop (sched-0351). → **PR #363** (2026-09-24, `0.10.0-pilot`, buses-data OA-363: in the admin console, a Customers or Users row with changes that are not saved is tinted and its Save button is enabled only then. Switching tab asks first, a reload or close is guarded, and a redraw keeps an unsaved edit instead of putting the server's value back. The Users name input is full width, and grid headings wrap instead of clipping). Admin page script, CSS and shell only: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+903519f` before it, which is `origin/main`, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+903519f` to the merge carrying this entry, and `/app/unsaved-edits.js` must be served. Deployed by the scheduled loop (sched-0439). → **PR #365** (2026-09-24, `0.10.0-pilot`, buses-data OA-152: `propose-update.mjs --no-notify` stages a refresh without emailing the customer, and `npm run deliver` forwards the flag). The change is `02bfc00`; the deployed commit is the squash merge carrying this entry. It changes a CLI script, a test, R4 and a changelog fragment only: no engine file, no stored sheet, no route, no server code and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+347158c` before it, which is `origin/main`, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+347158c` to the merge. Nothing a visitor sees changes. Deployed by the scheduled loop (sched-1152). → **PR for buses-data OA-165** (2026-09-24, `0.10.0-pilot`: re-vendors `engine/place/gen_internal.js` and `engine/poi_select.js` from claude-skills#118, so the simplified street map keeps a landmark's `force` but ignores its geographic `label.offset` and `label.anchor`). The change is the branch `oa165/vendor-schematic-label-offset`; the deployed commit is the squash merge carrying this entry. It moves engine files, their `vendored.json` hashes, the Aldi place fixture's `internal-schematic.svg` and a changelog fragment: no stored sheet, no route and no `SCHEMA_VERSION` move. Published maps change at their NEXT render only, and only once `track-engine.mjs` has run on the host (§4a, Peter's). The live site read `0.10.0-pilot+9423c31` before it, which is `origin/main`. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+9423c31` to the merge. Deployed by the scheduled loop (sched-1229). → **PRs #367 and #368** (2026-09-24/25, `0.10.0-pilot`: re-vendors for buses-data OA-416, `design.exitCaptionsInPanel`, and OA-250 item 2, one placer id per POI). Both went live without an entry of their own; this line is written after the fact, from the one reading that survives, `X-App-Version` reading `0.10.0-pilot+f6bcbcb` on 2026-09-25. → **PR #369** (2026-09-25, `0.10.0-pilot`: the FAQ's `#landmarks` answer no longer says pubs are never drawn, because buses-data OA-340 made pubs and allotments opt-in per map through `poi.include`; buses-data CORR-003). The change is `8759c72`; the deployed commit is the squash merge carrying this entry. One sentence of `public/faq.html`: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+f6bcbcb` before it, which is `8759c72`'s parent, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+f6bcbcb` to the merge, and `/faq.html#landmarks` must carry the sentence *Pubs and allotments are not drawn unless a map asks for them*. → **PR #333** (2026-09-25, `0.10.0-pilot`: dependabot's routine group — `fastify` 5.12.3 → 5.12.5 and `@fastify/static` 10.1.3 → 10.1.4, the latter a security release for GHSA-r799-r9gc-m956 / CVE-2026-90982, a route-guard and `allowedPath` bypass on case-insensitive filesystems; the host is Linux, so the bypass is not believed reachable there, and it ships anyway). The change is `e2f3a35`; the deployed commit is the squash merge carrying this entry. `package.json` and `package-lock.json` only: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+cd8dff7` before it, which is `e2f3a35`'s parent, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+cd8dff7` to the merge; `npm run check:live-routes` is the one that would see a route the framework bump lost. → **PR #364** (2026-09-25, `0.10.0-pilot`: the FAQ answers *Can we put the map on our own website?* — yes, by linking to `/m/<slug>` and to its service list; a framed embed is not available yet; rung 0 of the embed ladder, approved by Peter as drafted; buses-data OA-257). The change is `fcebcf0`, deployed by the scheduled loop's `sched-1315` tick; the deployed commit is the squash merge carrying this entry. One new `<details>` in `public/faq.html`: no engine file, no stored sheet, no route and no `SCHEMA_VERSION` move. The live site read `0.10.0-pilot+93f7e8e` before it, which is `origin/main` at the time of writing, so nothing else is waiting to go with it. **The §3a reading that can see it is `X-App-Version`**, which must move off `0.10.0-pilot+93f7e8e` to the merge, and `/faq.html` must carry `id="your-own-website"`.

**A caution about every `gitSha` quoted above, added 2026-08-22.** Several entries in this history quote a post-deploy `gitSha` from `/health?deep=1`, but `gitSha`, `builtAt` and the per-check `checks{}` are gated behind `opsAuthorised()` — a `METRICS_TOKEN` Bearer header (the `?token=` query form this originally also named was removed on 2026-08-25, N7), or an admin session. `scripts/deploy.mjs` step 5 curls that endpoint **with no token**, so it receives the short form (`status`, `service`, `version`, `time`) and never the three fields its own closing line instructed the operator to read. The deploy therefore never once confirmed *which commit* it had just put live: the check could not fail, because the values were absent rather than wrong. Fixed the same day — step 5 now prints the sha it resolved and built with, passes `METRICS_TOKEN` when one is set, and says plainly when the gate is the reason a field is missing. **Confirm a deploy by fetching something only the new build serves**, as was done for `b0c94ee`; treat a tokenless `/health` as a liveness check and nothing more.

### Running the upgrade, as actually done (2026-08-12)

Four steps, about five minutes. **Back up first, build second, switch third** — the order matters:

1. `docker compose run --rm backup` — takes ~30 s and writes to `<app dir>/backups/`. Do it even though cron backs up daily: a release that carries a migration wants a backup from *immediately* before it, not from 03:15.
2. `git pull`, and **read the new HEAD** rather than assuming the pull got what you expect.
3. `export GIT_SHA=$(git rev-parse --short HEAD) BUILT_AT=$(date -u +%FT%TZ) && docker compose build portal`. Those exports are not decoration — `compose.yaml` passes them as **build args** and the Dockerfile bakes them in, so without them `/health` reports `gitSha: unknown` and you lose the one reliable way to tell what is running. Building is safe: the old container serves throughout, so a failed build costs nothing.
4. `docker compose up -d portal` — the only step the public notices, a few seconds of 502 while the container is recreated.

Then check `/health?deep=1` for the expected `version` + `gitSha` and four green checks, and read the startup log. **Migrations run at import of `src/db/index.js`, before the server binds a port**, so a migration failure crash-loops the container rather than logging quietly — a healthy start with `database: ok` is itself the evidence that a migration succeeded.

**Rollback** is the same loop against the previous SHA (`git checkout <sha>`, rebuild, `up -d`). Schema changes here have been additive (`ALTER TABLE … ADD COLUMN`), and the older code's `SELECT *` tolerates an unknown column, so the database is usable by either build.

**Since 2026-08-25 that is tested rather than asserted** (`technical-audit_2026-08-25` N13). `scripts/test-schema-compat.mjs`, in `npm test`, copies `src/db/` out of the previous commit that touched it — the schema you would actually roll back to — and runs BOTH directions in child processes: this code against a database that release built, and that release against a database this code built. It asserts `SELECT *` on every table the app reads, in both directions. Nothing here needs an install, because `src/db/index.js` imports only node builtins. **If you write a migration that is not additive, that gate goes red before the deploy, not during the rollback.** It reports SKIPPED on a shallow clone, which is why the tests workflow checks out with `fetch-depth: 0`.

**What a rollback does to sessions, deliberately.** The `session.token` column holds a SHA-256 since N3 and keeps its old NAME so that older code compares a raw cookie against a hash, matches nothing, and everybody signs in again — a rename would leave a MISSING column and throw on every request at the worst possible moment. The test asserts exactly that: the old release opens the database, reads every table, and simply does not match the session.

**And the database says which generation it is.** `schema_version` (one row) records the `SCHEMA_VERSION` of the code that last migrated it. Reading a database written by a NEWER release — precisely what a rollback leaves behind — logs a warning naming both numbers and carries on. It is never a refusal: an app that will not boot after a rollback turns a bad ten minutes into an outage. **Bump `SCHEMA_VERSION` in `src/db/index.js` when you add a migration.** That sentence was advice and nothing else until 2026-09-12, when PR #282 added an `ALTER TABLE` and left the constant at 4 with the whole suite green; `scripts/test-schema-version.mjs` now holds the numbered version blocks above the constant to the migrations below it, so a migration no block describes is a red test rather than something a reader has to remember (buses-data OA-325).

**Two traps for an AI assistant driving this:**

- **Claude Code's auto-mode classifier refuses a raw `ssh` or `scp` to this host** (as it refuses `curl` POSTs to production). Since 2026-09-17 (R1) an agent may run the deploy itself, as `npm --prefix "C:/Claude/community-bus-maps" run deploy`, under the conditions in [§3b](#3b-the-deploy-history-entry-is-written-before-the-deploy-and-merged-with-it) — CI green on `main`, the history entry merged first, `X-App-Version` and `/health?deep=1` read back, a failed read-back reverted by a second pull request. Every hand-run `ssh`/`scp` step, including the recipe in this section, stays with the operator: hand over the command and read back the output. Do not attempt to work around a refusal.
- **Windows PowerShell 5.1 strips embedded double quotes** when calling a native exe like `ssh`, so a remote command containing `grep -iE "a|b"` arrives at bash as `grep -iE a|b` and bash reads those `|` as pipes ("command not found"). Keep the remote command in a **single-quoted** PowerShell string (so `$(…)` and `$VAR` survive for the remote shell), and inside it avoid double quotes entirely — use `grep -i -e pat1 -e pat2`, where each pattern is its own argument. Also: `$host` is a reserved PowerShell variable; use `$target`.

- **Known gotcha if you ever recreate the data volume:** it defaults to `root:root`, but the
  container runs as the unprivileged `node` user (uid 1000, same as the host's `ubuntu` user, that's
  not a coincidence worth relying on elsewhere) — `chown -R node:node` on `/data` and `/backups`
  before the first start, or the container crash-loops on `EACCES` opening `portal.sqlite`.
