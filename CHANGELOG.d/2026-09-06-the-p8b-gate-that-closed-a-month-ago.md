---
date: 2026-09-06
title: "P8b was waiting on a sign-off resolved a month earlier, in a note ten lines further down the same file"
---

buses-data OA-086, phase 0 of `Buses/Development Docs/portal-embed-offer-plan_2026-09-06.md`. Documentation only — no code, no route, no header changed.

- **`docs/ROADMAP.md` contradicted itself about P8b's gate, in one file.** The P8b row named two preconditions, "the DPA/processor wording and the bustimes.org sign-off", while the launch-gate note further down the same document recorded the bustimes.org terms check as **resolved on 2026-08-07** — the site owner confirmed the use acceptable with no attribution required, `docs/LICENSING.md` §3, where its review table has said `resolved` ever since. Nothing propagated the closure back to the row that was waiting on it, in either repository. The row now names one gate.
- **The remaining gate is not "not on code", which is what the row claimed.** The processor paragraph cannot be written honestly while `src/server.js`'s request serialiser records `remoteAddress: req.ip` for every request and Caddy's JSON access log records the client address into a file the `Caddyfile`'s own comment describes as covered by no retention rule — and while `public/legal.html` tells the public that Peter "is the data controller for everything described on this page. There is no other organisation involved." An embed makes that sentence false for a council's visitors. The wording waits on a logging decision, not the other way round.
- **Two things the row did not know, recorded so the next reader gets them for free.** A per-map `frame-ancestors` allowlist **cannot be served by the app**: the `Caddyfile`'s `header` block sets `X-Frame-Options: DENY` and a CSP carrying `frame-ancestors 'none'` on every response for `busmaps.uk`, and that directive replaces rather than appends — so P8b's security-critical piece lands in the one file nothing deploys automatically and no test exercises, and `scripts/deploy-caddy.mjs`'s read-back checks header *names* on the apex only. And P8a — the linkable page with its text alternative — is the cheaper half of the same customer question, shipped weeks ago and advertised nowhere: `public/faq.html` answers "does our map get a public web page?" and nothing answers "can we put it on ours?".
- **Nothing here changes the plan's status.** P8b stays `⏳ planned` and stays gated on buses-data's OA-014 trigger, the first real pilot customer. The correction makes the row describe what it is actually waiting for.
