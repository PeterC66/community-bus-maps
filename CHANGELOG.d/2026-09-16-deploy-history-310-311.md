---
date: 2026-09-16
title: "Deploy history: #310 and #311, written BEFORE the deploy per DEPLOY.md 3b"
---

- **The entry for the place-index fix and the five `/maps` search findings**, written before the deploy so the commit that goes out is the merge carrying it. `git log 08d1b19..origin/main` held exactly the two commits the entry describes, checked at the moment of writing — the two previous entries both had to carry a correction saying the list under-described what went live, so the check is now part of writing one.

- **It records something the readings below would otherwise not make sense of**: `scripts/build-place-index.mjs` was run on the host *before* this deploy, to repair the four St Neots maps that were published with no place-name sidecar. Those sidecars are already on disk and invisible, because the search index is built once per process — so the container recreation is what makes them live, not any code in this release.

- Seven live readings taken at 05:34Z, all zero, and one reading that must **not** move: `?q=Eynesbury` still returns no St Neots map, because nothing here indexes a map's internal sheet. Written down so a later reader does not take a known remainder for a regression.
