---
date: 2026-08-27
title: "The dark register is a grep, so every member has to match it"
---

- **Four more engine files re-vendored, comments only.** Two of the eleven `DARK, measured` notes shipped an hour earlier opened with their own wording — "DARK BELOW THE v2 BRANCH" and "DARK IN THE ONLY SENSE THAT MATTERS" — so `grep -rn "DARK, measured" engine/` found twelve entries and silently missed two. An enumeration that does not enumerate is worse than none, because it answers. Both normalised.
- **Two sites that had no note at all now have one.** `feature_labels.js`, where every fault path of all four guards is taken by no committed map — that is the guards working, since each was written after a shipped sheet went wrong and a fixed board trips nothing — and `svg_primitives.js`, where `design.badgeFit:false` is set by no map, so the whole opt-out from the stadium badge is certified by a unit test alone. Sixteen entries across eight modules, all matching one grep.
- Byte-inert: `verify:place` reproduces all four sheets across both fixtures byte-for-byte, and the vendoring audit accounts for all 28 engine files.
