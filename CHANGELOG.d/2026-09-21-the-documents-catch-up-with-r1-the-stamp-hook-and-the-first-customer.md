---
date: 2026-09-21
title: "The documents catch up with R1, the stamp hook and the first customer"
---

- **`docs/DEPLOY.md` said an agent cannot deploy, and that has been false since 2026-09-17** (buses-data OA-394, R1 of the process review). §3b now opens with *Who runs this*: a session or the scheduled loop may run `npm --prefix "C:/Claude/community-bus-maps" run deploy` with CI green on `main`, the history entry merged first, `X-App-Version` and `/health?deep=1` read back, and a failed read-back reverted by a second pull request. What stays Peter's is said as plainly: every raw `ssh`/`scp` — the token-holding read-back in §4, the two `track-engine.mjs` commands in §4a, the §5 drills — because the auto-mode classifier refuses them to sessions. A pending deploy is a worklist chore (`deploy_pending.mjs`), never a red.
- **§9 no longer says the site "isn't serving yet"**, cites the archived go-live record as history, says the Caddyfile is deployed by `npm run deploy:caddy`, and gives the backup pull as hourly (since 2026-08-30), not daily at 08:00.
- **`docs/DEVELOPING.md` and the pre-commit hook's own comment stop describing the Stop hook.** The stamp is written at commit time by `docstamp.py --staged` (buses-data OA-397, portal #315); the re-stamp-before-you-commit procedure and the "Known rough edge" that `check-vendored` had already fixed are deleted.
- **`docs/H2-todo-quickstart.md` drops the `cbm_session` cookie procedure** for the read-only `BUSMAPS_TOKEN` that `worklist.mjs` has used since OA-203, and gives the local portal as `127.0.0.1:5180`, which is where `src/config.js` binds.
- **"Four outputs" becomes five** wherever it was stated (the boarding plan joined the four; the diagram is parked), **`npm run verify` is described as gating the committed `gate-fixtures/`** rather than needing `FIXTURE_DIR` and skipping, and **"there are no customers" becomes "the pilot has its first customer (September 2026)"** in CLAUDE.md, the README, PRODUCT, PILOT, ROADMAP, H1, R2, R6, DEVELOPING, DUMMIES_GUIDE, LICENSING and `package.json`. Also: the README's *Submit for publication* label (the button reads *Send ‹version› for review*), its data-hygiene rule (gate fixtures and the engine are committed deliberately), `/legal.html` described as a working draft, PILOT's *"None are sent"* about email, and PILOT's `/srv/busmaps` host path.
