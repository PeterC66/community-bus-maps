{
  "name": "community-bus-maps",
  "version": "0.0.1",
  "private": true,
  "description": "Self-serve portal for community bus maps — public shopfront plus a deterministic map renderer. Apache-2.0.",
  "type": "module",
  "license": "Apache-2.0",
  "author": "Peter Cooper",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "verify": "node --env-file-if-exists=.env scripts/verify-reproduce.mjs"
  },
  "dependencies": {
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.1",
    "sharp": "^0.34.5"
  }
}
